# 04 — Nhật ký & Inventory tùy biến (vs upstream)

> **Mục đích:** theo dõi MỌI khác biệt so với `Tencent/WeKnora` để quản lý `git merge upstream` và bàn giao.
> **Quy tắc cho AI agent:** khi sửa **file dùng chung của upstream** hoặc **thêm migration/schema**, PHẢI thêm dòng vào bảng phù hợp trong cùng PR.

## Thông tin fork
- **Upstream:** `https://github.com/Tencent/WeKnora` · **Origin:** `github.com/covuaduongsinh/WeKnora_chess`
- **Nền:** v0.6.2 · **Nhánh:** `main` · **Production:** `weknora.covuaduongsinh.com`
- **Điểm rẽ nhánh (merge-base) lúc lập inventory:** `5d0d317a` (2026-06-22). Upstream tip khi đó: `7d8a80ae` (2026-06-28).
- **Quy mô tùy biến (diff `upstream/main...HEAD`):** ~75 file lớp cờ (mới) · **76 file dùng chung bị SỬA** · 1 file bị XÓA · 39 file mới non-chess (deploy/lite/i18n).
- **Cập nhật lại inventory:**
  ```bash
  git fetch upstream main --no-tags
  git diff --name-status -M upstream/main...HEAD     # phần fork đã đổi so với điểm chung
  ```

---

## A. Lớp cờ — code RIÊNG (rủi ro conflict THẤP, ~75 file)
| Vùng | Đường dẫn |
|---|---|
| Engine | `internal/chess/` (board, engine, uci_engine, http_engine, *_test) |
| Agent tools | `internal/agent/tools/chess_*.go` (6 tool + common + demo_test) |
| Repository | `internal/application/repository/chess_*`, `wiki_chess_ref.go` |
| Service | `internal/application/service/chess_*` |
| Handler | `internal/handler/chess_*` |
| Types | `internal/types/(interfaces/)chess_*`, `wiki_chess_ref.go` |
| Frontend | `frontend/src/views/chess/**`, `views/chat/components/tool-results/ChessBoardDisplay.vue`, `api/chess/`, `stores/chessWikiDraft.ts`, `utils/chessBlocks.ts`, `utils/chessRef.ts` |
| Docker engine | `docker-compose.chess.yml`, `docker/Dockerfile.chess-engine`, `docker/chess-engine/uci_http_bridge.py` |
| Deploy/Docs | `scripts/deploy/weknora-chess.service`, `scripts/seed_chess_wikilink_demo.*`, `docs/chess-wikilink-demo.md` |

## B. Migrations cờ (NỐI TIẾP upstream) — `000062`–`000072`
courses · games_puzzles · slugs · wiki_chess_refs · course_slug · refs_source_type · slug_aliases · kb_index · `000070` chess_positions (Ngân hàng thế cờ) · **`000071` chess_books** (thực thể mới — Thư viện sách: `chess_shelves`, `chess_books`, `chess_shelf_books` pivot nhiều-nhiều, `chess_book_chapters`, `chess_book_images`, `chess_chapter_revisions`; xem mục E) · **`000072` chess_articles** (thực thể mới — Ngân hàng bài viết: `chess_articles`, `chess_article_topics`, `chess_article_topic_items` pivot nhiều-nhiều, `chess_article_images`, `chess_article_revisions`, + `ALTER TABLE chess_slug_aliases ADD COLUMN kind` — xem mục E).
> Khi merge: nếu upstream thêm migration trùng dải số → đổi số migration cờ cao hơn.

---

## C. FILE DÙNG CHUNG ĐÃ SỬA (rủi ro conflict CAO) — 76 file
*(số "N×" = số lần xuất hiện chữ "chess" trong file, đo lúc lập inventory — càng cao càng là điểm móc lõi.)*

### C1 — Móc nối backend (BẮT BUỘC để ý khi merge)
| File | Vai trò móc nối cờ | Mức |
|---|---|---|
| `internal/application/service/agent_service.go` | Gắn agent/tool cờ vào luồng agent. **WS1 (nối Puzzle Bank):** +field `chessLibraryService` + param `NewAgentService` + truyền vào `NewChessGeneratePuzzleTool(s.chessLibraryService)` để tool ra bài tập từ DB. | 32× |
| `internal/config/config.go` | Đọc `WEKNORA_CHESS_*` (engine + `CHESS_EVALUATE_*`/`CHESS_LOOKUP_OPENING_LIMIT`). ⚠️ **KHÔNG** đọc `CHESS_KB_INDEX` — gate đó đọc thẳng bằng `os.Getenv` trong `chess_knowledge_indexer.go` (`Enabled()`), đừng tìm nhầm ở đây | 23× |
| `internal/agent/tools/definitions.go` | Đăng ký 6 tool `chess_*` vào registry | 21× |
| `internal/router/router.go` | Mount route `/api/v1/chess/...`. **WS2:** +nhóm `/chess/library` với `POST /reindex` (backfill KB cờ). **WS-RAG:** +`GET /chess/library/index-status` (chẩn đoán RAG, Contributor). | 17× |
| `internal/container/container.go` | Wiring DI service/repo cờ | 11× |
| `internal/types/custom_agent.go` | Field phục vụ agent cờ | 3× |
| `config/builtin_agents.yaml` | Agent `builtin-chess-coach` | (block cuối) |
| `internal/agent/act.go` | **0× chess** — sửa vì lý do khác (rà lại khi merge) | 0× |

### C2 — Tích hợp Wiki ↔ wikilink cờ
| File | Vai trò | Mức |
|---|---|---|
| `internal/application/service/wiki_page.go` | Trang wiki nhận diện/giải link cờ | 39× |
| `internal/types/wiki_page.go` | Kiểu dữ liệu wiki + ref cờ | 15× |
| `internal/application/service/wiki_ingest.go` | Expand wikilink khi embed | 1× |
| `internal/application/service/wiki_lint.go` | Lint wiki có ref cờ | 1× |
| `frontend/src/views/knowledge/wiki/WikiBrowser.vue` | Hiển thị node cờ trong graph | — |

### C3 — Móc nối frontend
| File | Vai trò | Mức |
|---|---|---|
| `frontend/src/types/tool-results.ts` | Kiểu kết quả tool cờ | 8× |
| `frontend/src/views/chat/components/ToolResultRenderer.vue` | Render bàn cờ từ kết quả tool | 4× |
| `frontend/src/router/index.ts` | Route khu "Quản lý cờ vua" | 3× |
| `frontend/src/components/menu.vue` | Mục menu cờ | 2× |
| `frontend/src/stores/menu.ts` | State menu | 1× |
| `frontend/src/utils/agent-tool-icons.ts` | Icon cho tool cờ | 1× |
| `frontend/src/main.ts` | **+1 dòng** import `assets/theme/duongsinh-responsive.css`, phải đặt CUỐI khối CSS (cùng khuôn `duongsinh-brand.css`) | — |
| `frontend/src/views/platform/index.vue` | **+4 dòng** (`import MobileNavBar` + `<MobileNavBar />` đặt TRƯỚC `<Menu>`) — MỚI vào inventory từ đợt giao diện điện thoại | — |
| `frontend/src/views/chat/index.vue` | **~8 dòng**: `.chat` bỏ `max-width: calc(100vw - 260px)` + `min-width: 400px` + khối `&.is-sidebar-collapsed` → `max-width: 100%; min-width: 0`. MỚI vào inventory | — |

### C4 — i18n & prompt templates (Việt hóa + bỏ tiếng Trung)
- **Bỏ** `frontend/src/i18n/locales/zh-CN.ts` (D) · **Thêm** `locales/vi-VN.ts` (A).
- Sửa: `i18n/index.ts`, `i18n/embed.ts`, `locales/en-US.ts`, `locales/ko-KR.ts`, `locales/ru-RU.ts`.
- Prompt: `config/prompt_templates/` → `agent_system_prompt.yaml`, `context_template.yaml`, `fallback.yaml`, `intent_prompts.yaml`, `rewrite.yaml`, `system_prompt.yaml`.
> Đây là vùng dễ conflict text khi upstream đổi prompt/i18n — thường tự giải quyết được, nhưng kiểm tra kỹ vi-VN không bị mất.

### C5 — Hạ tầng / build / deploy (khác biệt lớn ngoài lớp cờ)
- **Build/dep:** `Makefile`, `docker/Dockerfile.app`, `go.mod`, `go.sum`, `frontend/package.json`, `frontend/package-lock.json`, `frontend/index.html`, `frontend/env.d.ts`, `.gitattributes`, `.env.example`.
- **CI/CD & deploy riêng (mới, A):** `.github/workflows/cicd-deploy.yml`, `docker-compose.caddy.yml`, `docker-compose.ghcr.yml`, `docker-compose.override.yml`, `docker/caddy/Caddyfile`, `scripts/deploy/{backup,restore,dc,pull-deploy,redeploy,server-bootstrap}.sh`, `scripts/deploy/hetzner-cloud-init.yaml`, `scripts/dev/{local-deploy,local-status}.sh`, `scripts/dev/githooks/pre-push`, `docs/deploy/{backup-restore,cicd,dev-workflow,hetzner,https}.md`. Sửa: `.github/workflows/docker-image.yml`.
- **mcp-server:** sửa `main.py`, `run_server.py`, `run.py`, `setup.py`, `test_imports.py`, `test_module.py` + thêm `uv.lock`, `*.egg-info/*`.

### C6 — Bản chạy SQLite ("lite") — mới
- Sửa `migrations/sqlite/000000_init.up.sql`; **thêm** `migrations/sqlite/000001..000003*` (wiki_and_indexing, lite_missing_tables, messages_attachments), `build_sqliteshim/*.h`, `frontend/src/utils/fileTransfer.ts`.
- Liên quan: `.env.lite`, `weknora-lite.log` ở root.
> Fork có nhánh chạy **SQLite** thay PostgreSQL (bản nhẹ) — lưu ý khi merge migration upstream (chỉ có bản postgres).

### C7 — File dùng chung khác bị sửa (không chứa "chess")
`internal/im/{cmd_clear,cmd_help,cmd_info,cmd_search,cmd_stop,service}.go`, `internal/im/feishu/adapter.go`, `internal/errors/errors.go`, `internal/middleware/language.go`, `internal/types/{context_helpers,context_helpers_test,placeholder}.go`, `frontend/src/App.vue`, `frontend/src/views/auth/Login.vue`, `frontend/src/api/chat/streame.ts`, `frontend/src/api/embed/index.ts`, `frontend/src/components/{AgentEmbedChannelPanel,manual-knowledge-editor,MyInvitationsDialog}.vue`, `frontend/src/views/chat/components/{AgentStreamDisplay,botmsg}.vue`, `frontend/src/views/chat/components/tool-results/WebSearchResults.vue`, `frontend/src/views/settings/{GeneralSettings,TenantInfo,TenantMembers,UserProfile}.vue`, `frontend/src/views/system/SystemSettings.vue`, `frontend/src/utils/request.ts`, `data/weknora.db.bak-131441`.
> Phần lớn là Việt hóa/tinh chỉnh UI/IM. Rà từng cái khi conflict; ưu tiên giữ thay đổi tối thiểu.

---

## D. Quyết định kiến trúc (ADR rút gọn)
- **Engine:** Arasan (MIT) sidecar **HTTP** (UCI→HTTP bridge), gọi qua `WEKNORA_CHESS_*`.
- **Agent HLV:** `kb_selection_mode: none` — mặc định KHÔNG RAG, chỉ 7 tool cờ + engine (thêm `chess_lookup_position` từ đợt Ngân hàng thế cờ, mục E). *(Bật `CHESS_KB_INDEX` + thêm `knowledge_search` để trích dẫn lý thuyết/sách.)*
- **RAG cờ:** gate `CHESS_KB_INDEX` (mặc định TẮT); import PGN hàng loạt KHÔNG trigger index.
- **Wikilink:** slug bất biến; resolve `exact → alias → fuzzy` (bigram-Jaccard ≥ 0.8); bảng `chess_slug_aliases`.
- **i18n:** bỏ `zh-CN`, chuẩn hóa **vi-VN** làm ngôn ngữ chính.
- **Triển khai:** thêm bản **SQLite "lite"** + bộ **deploy Caddy/Hetzner/CI-CD** riêng (ngoài upstream).

## E. Backlog tùy biến

### Tiến độ thực thi plan "nối dây + đổ xăng" (2026-06-29)
- [x] **WS1** — Nối Puzzle Bank → `chess_generate_puzzle` (tool đọc DB qua `PuzzleSource`/`RandomPuzzle`, fallback embedded). Sửa `agent_service.go` (C1). Test: `chess_generate_puzzle_test.go`.
- [x] **WS3** — Mở rộng khai cuộc: nhúng `data/eco.tsv` (3733 khai cuộc, lichess CC0) qua `chess_openings_data.go`; `openingIndex` gộp dataset + overlay Việt hoá; `chess_lookup_opening.go` dùng index. Test: `chess_lookup_opening_test.go`.
- [x] **WS2 (chuẩn bị, CHƯA bật)** — `ReindexAll` (service+interface chess) + route `POST /chess/library/reindex` (router.go C1) + runbook `docs/chess-rag-enable.md`. Bật runtime do Thầy theo runbook.
- [x] **WS4b** — thông báo lỗi engine thân thiện (`friendlyEngineError` trong `chess_common.go`, áp cho analyze/best_move/explain_move); `httpEngine.Health()` probe + cảnh báo sớm lúc init trong `getChessEngine` (agent_service.go C1). Test: `chess_engine_error_test.go`. (Resolve fuzzy đã có test sẵn `chess_resolve_test.go`.) *Còn nợ:* endpoint `/chess/engine/health` (cần refactor engine thành service DI) — để sau.
- [x] **WS4a** — áp thương hiệu Dương Sinh: file đè `frontend/src/assets/theme/duongsinh-brand.css` (thang `--td-brand-color-*` → navy #2B3990, light+dark) import sau theme.css ở `main.ts`/`embed-main.ts` (C3 frontend); `index.html` đổi title + favicon SVG + meta; logo `frontend/public/duongsinh-{symbol,logo}.svg` (từ `@ds/brand`). Build frontend OK. *Lưu ý:* brand thật là navy + xanh, KHÔNG cam/gold (đã đính chính `01-du-an-duongsinh.md`).

### Đợt củng cố sau audit (2026-06-29) — nhánh `feat/chess-audit-hardening`
Rà soát toàn dự án (3 agent: backend/frontend/hạ tầng) → triển khai 4 nhóm A–D.
- [x] **WS-A (vận hành & an toàn):**
  - Thêm `migrations/versioned/000068_*.down.sql` + `000069_*.down.sql` (trước đó thiếu cặp `.down` → không rollback được). Mục B nhật ký.
  - `docker-compose.chess.yml` (file cờ riêng): thêm `healthcheck` cho `chess-engine` (probe `/health`) + `app.depends_on` đổi sang `condition: service_healthy`.
  - `.gitignore`: thêm `data/*.bak*`; `git rm --cached data/weknora.db.bak-131441` (gỡ khỏi track, giữ file local).
  - `.github/workflows/cicd-deploy.yml` (file CI riêng, C5): thêm job `test` (go vet+test package cờ, frontend type-check) làm gate trước `deploy`.
- [x] **WS-B (củng cố backend):**
  - Test 4 tool: thêm `chess_analyze_position_test.go`, `chess_best_move_test.go`, `chess_explain_move_test.go`, `chess_evaluate_game_test.go` (tái dùng `stubEngine`/`errEngine`, thêm `emptyMoveEngine`).
  - Gom trùng lặp: `validateFENArg` trong `chess_common.go` (áp analyze/best_move/explain_move); gộp `chessSideToMove`→`fenSide` (xóa bản trùng ở demo test); lookup dùng `fenSide`.
  - Config hóa: thêm `EvaluateDepth`/`EvaluateMaxPlies`/`LookupOpeningLimit` vào `ChessConfig` (`config.go` C1) + env `CHESS_EVALUATE_DEPTH`/`CHESS_EVALUATE_MAX_PLIES`/`CHESS_LOOKUP_OPENING_LIMIT` (`.env.example`); constructor tool nhận tham số; `agent_service.go` (C1) thêm getter + truyền vào `NewChessEvaluateGameTool`/`NewChessLookupOpeningTool`.
  - Endpoint `GET /chess/engine/health`: file MỚI `internal/application/service/chess_engine.go` + `internal/handler/chess_engine.go` + `internal/types/interfaces/chess_engine.go`; wiring `container.go` (C1, +2 Provide) + route `router.go` (C1, `RegisterChessEngineRoutes`, Viewer). **Quyết định:** làm service ENGINE ĐỘC LẬP (không sửa `NewAgentService`) để giữ file xung đột cao nhất tối thiểu — engine http là wrapper rẻ nên chấp nhận init lười riêng. Trả `{enabled, healthy, detail}`.
- [x] **WS-C (frontend/brand):**
  - Font Roboto self-host (Apache-2.0): 9 file woff2 (latin/latin-ext/vietnamese × 400/500/700) ở `frontend/public/fonts/` + `@font-face` trong `duongsinh-brand.css`. Build OK, dist copy đủ.
  - Họa tiết ô cờ (checkerboard rất nhạt) cho `.chess-manage` (scoped, không đụng global).
  - Validate FEN khi nhập: `isValidFEN()` trong `utils/chessBlocks.ts` (validator cấu trúc, không import chess.js để khỏi phình bundle chat); áp ở `PuzzleBank.vue` + `ChessCourses.vue` (FEN bài học, chỉ khi nhập).
  - Type-safe: `PuzzleDialogState`/`CourseDialogState`/`LessonDialogState` thay `reactive<any>`; fix viewer PuzzleBank cập nhật sau khi sửa (revealKey++).
  - a11y: `menu.vue` (C3 shared) thêm `alt="Cờ vua Dương Sinh"` cho logo.
  - **CI đính chính:** `npm run type-check` (vue-tsc) ĐANG đỏ do lỗi type tồn đọng ở phần upstream (KnowledgeBase/ApiInfo/GraphSettings…) → ĐÃ BỎ khỏi gate `cicd-deploy.yml` (giữ gate Go; frontend vẫn dựng qua `vite build` ở build-ui). Bật lại type-check khi dọn sạch type upstream.
  - **HOÃN C3** (tách `ChessCourses.vue` 637 dòng): refactor thuần nội bộ, không thêm giá trị; picker gắn chặt template-ref `<textarea>` nên tách "mù" (không chạy được app để test runtime tại đây) dễ hồi quy → để lại backlog, làm khi chạy app kiểm thử được.
- [x] **WS-D (tính năng nợ):**
  - **D1 — RAG cờ:** runbook `docs/chess-rag-enable.md` đã khớp code; thêm mục **3b Production** turn-key (backup DB trước → set `CHESS_KB_INDEX=true` → redeploy → reindex prod URL → sửa agent YAML → nghiệm thu). KHÔNG commit đổi `builtin_agents.yaml` (toggle vận hành; flip sớm sẽ đổi hành vi HLV + `kb_selection_mode:all` kéo mọi KB trước khi KB cờ sẵn sàng) — Thầy bật theo runbook. Bật production là thao tác trên server (ngoài tầm tác động của agent ở đây).
  - **D2 — đổi tên slug:** thêm repo `UpdateGameSlug`/`UpdatePuzzleSlug` (tách riêng vì `UpdateGame`/`UpdatePuzzle` cố tình KHÔNG đụng cột slug); service `RenameGameSlug`/`RenamePuzzleSlug` (chuẩn hóa qua `slugifyChess` + `ensureUniqueChessSlug` + ghi `chess_slug_aliases` old→new); handler + route `PUT /chess/{games,puzzles}/:id/slug` (router.go C1, Contributor); frontend api `renameGameSlug`/`renamePuzzleSlug` + nút "Đổi slug" (prompt) ở `GameLibrary.vue`/`PuzzleBank.vue`. Bảng `chess_slug_aliases` (000068) nay được dùng → resolve `exact→alias→fuzzy` có dữ liệu. *Còn lại:* rename cho course/lesson (service khác) — backlog.

### Đợt sửa RAG sau khi bật production (2026-06-30) — nhánh `feat/chess-audit-hardening`
RAG production bật xong nhưng agent "tìm thấy tên nhưng không ra nội dung". Rà 3 agent + đọc code → **nguyên nhân gốc:** reindex báo `success` GIẢ (đếm số ván của tenant, không phải số đã embed); embedding chạy NỀN; bị SKIP im lặng nếu KB cờ không có embedding model → chunk lưu DB nhưng không lên vector store → `knowledge_search` rỗng. Triển khai "hoàn thiện sâu":
- **Endpoint chẩn đoán** `GET /chess/library/index-status` (Contributor): file MỚI logic trong `chess_knowledge_indexer.go` (`IndexStatus`), lộ qua `chessLibraryService.IndexStatus` + handler `chess_library.go` + route `router.go` (C1). Trả `{enabled, kb_exists, embedding_configured, total, completed, pending, failed, sample_error}`. Struct `ChessIndexStatus`/`ChessReindexResult` đặt ở `internal/types/chess_kb_index.go` (tránh import cycle interfaces→service).
- **Reindex trung thực + fail-loud:** `IndexGame/IndexPuzzle/IndexLesson/upsert` (indexer) nay TRẢ `error` (caller CRUD vẫn best-effort qua `_ =`); `ReindexAll` đổi chữ ký trả `*types.ChessReindexResult`, **chặn trước** khi KB cờ thiếu embedding model (lỗi rõ thay vì success giả), đếm thật (total/enqueued/failed/errors). Handler giữ `games_indexed`/`puzzles_indexed` để tương thích ngược + thêm `note` nhắc kiểm tra index-status. Đổi interface `ChessLibraryService` (file cờ riêng).
- **Agent prompt** (`builtin_agents.yaml`, C1): khi `knowledge_search` rỗng → nói "kho tri thức chưa có tài liệu khớp" + trả lời bằng kiến thức cờ + công cụ cờ; **CẤM bịa tên file/đường dẫn** (gemini-2.5-flash từng confabulate "Opera Game.md"). Threshold đã hạ trước đó (vector 0.3/rerank 0.1) — giữ.
- **Runbook** `docs/chess-rag-enable.md`: thêm mục **4b** bảng chẩn đoán theo `index-status` + SQL fallback.
- *Đa số diff trong file `*chess*` (an toàn merge); file dùng chung: `router.go` (1 route), `builtin_agents.yaml` (prompt).*

**Bổ sung sau khi soi LOG production (lỗi thật là CONFIG, không phải vector store):** Log hiện `ERROR tool not found: knowledge_search` + `knowledge_search tool not enabled` + `RetrieveKBOnlyWhenMentioned=true` (YAML set false). → (1) Effective config của chess-coach KHÁC YAML (builtin agent bị chỉnh qua UI → lưu DB → DB override YAML, xem `custom_agent.go:185-208`); bản DB thiếu knowledge_search nên LLM gọi (vì system_prompt nhắc tên) → "tool not found". (2) `session_agent_qa.go:112-116` chặn CỨNG: có knowledge_search mà thiếu `rerank_model_id` → lỗi; tenant Dương Sinh KHÔNG có rerank model (chỉ chat+embedding gemini). **Sửa (file dùng chung `internal/application/service/session_agent_qa.go` — MỚI vào inventory C7):** nới ràng buộc rerank thành TÙY CHỌN — thiếu rerank model thì cảnh báo + đi tiếp, để `knowledge_search` tự dùng LLM-based rerank fallback bằng chat model (fallback đã có sẵn `knowledge_search.go:287,301`). *Còn lại (vận hành):* xóa/ghi đè DB override của chess-coach để allowed_tools có knowledge_search (sửa qua UI hoặc xóa override).

**Bổ sung sau khi index-status chạy (vẫn rỗng dù 7/7 completed):** Đào tiếp tới `session_knowledge_qa.go` (KBSelectionMode=all) + `tools/capabilities.go`. **NGUYÊN NHÂN GỐC THẬT:** `knowledge_search` yêu cầu KB có capability `vector|keyword` (`KBSatisfiesAgentRequirements`); capability lấy từ `KnowledgeBase.IndexingStrategy.{Vector,Keyword}Enabled`. KB "Tri thức cờ vua" production có vector+keyword=FALSE → (a) bị loại khỏi tập search của agent VÀ khỏi @picker frontend; (b) `processChunks` skip embedding (`NeedsEmbeddingModel`=false) nên không có vector — `completed` chỉ là parse xong, KHÔNG đảm bảo đã embed. `ensureChessKB` tạo KB KHÔNG set `IndexingStrategy` (dựa `EnsureDefaults`); KB cũ trên prod dính false. **Sửa:** (1) `ensureChessKB` set TƯỜNG MINH `IndexingStrategy: types.DefaultIndexingStrategy()` khi tạo + cảnh báo nếu KB cũ tắt index; (2) index-status thêm `vector_enabled/keyword_enabled/searchable` + `enabled_docs/disabled_docs` để xác nhận; (3) runbook 4b: nhánh `searchable:false` → XÓA KB cờ + reindex (code mới tạo lại đúng). Vận hành: xóa "Tri thức cờ vua" trên prod → reindex → KB mới có vector+keyword bật → embedding chạy thật.

### Ngân hàng thế cờ FEN — position bank (2026-08-10) — nhánh `feat/chess-position-bank`
Thêm thực thể cờ THỨ 5 `position` (bên cạnh game/puzzle/lesson/course), wikilink `[[position/<slug>]]`. **Ranh giới:** puzzle = bài TẬP có lời giải, để LUYỆN (không đổi); position = thế cờ THAM CHIẾU để DẠY/trích dẫn/phân tích (tàn cuộc lý thuyết, tabiya khai cuộc, mô-típ chiến thuật/cấu trúc tốt) — **CỐ Ý cho phép FEN không có quân Vua** (thế cờ giản lược dạy trẻ mới học).

**Kiểm chứng ràng buộc "không Vua" trước khi code (không cần thoả hiệp):** `notnil/chess` (`decodeFEN`) không kiểm tra sự tồn tại của Vua, chỉ đòi đủ 6 trường + cấu trúc — nên `chess.ValidateFEN`/`isValidFEN` (frontend) dùng lại được nguyên vẹn. Ba nơi PHẢI tránh: (1) chess.js frontend bắt buộc đúng 2 Vua → `ChessBoardDisplay`/`ChessRefEmbed` dữ liệu position **không bao giờ set `pgn`**, chỉ `fen`; (2) engine Arasan không phân tích được → không gọi engine cho position; (3) `chess.FENAfterMove`/`UCIToSAN`/`gameFromFEN` dựng `notnil.Game` → không dùng cho position.

**Quyết định kiến trúc:** position là "ngăn" thứ ba của `chessLibraryService`/`chessLibraryRepository`/`ChessLibraryHandler` (file mới `chess_library_position.go`, `chess_position.go` repo/handler — method trên CÙNG struct), **không phải service riêng** → `container.go` **0 dòng thay đổi**; `router.go` chỉ thêm 1 nhóm route trong `RegisterChessLibraryRoutes` đã có.

- **Backend mới (file riêng, an toàn merge):** migration `000070_chess_positions` (mục B); `internal/chess/fen.go` (`NormalizeFEN` bù trường thiếu, `FENKey`, `HasBothKings`, `SideToMove` export) + test; `internal/types/chess_position.go`; `internal/application/repository/chess_position.go`; `internal/application/service/chess_library_position.go`; `internal/handler/chess_position.go`; `internal/agent/tools/chess_lookup_position.go` (tool thứ 7, mẫu `PuzzleSource`, có bộ thế cờ mẫu nhúng sẵn gồm 1 thế KHÔNG Vua) + test; `frontend/src/views/chess/PositionBank.vue` + `components/ChessPositionEditor.vue` (trình soạn thế cờ bằng chuột — cm-chessboard không validate luật gì, xác nhận qua đọc source; **không dùng chess.js**) + `frontend/src/utils/chessPositionOptions.ts`.
- **File dùng chung bị chạm (đã có sẵn trong inventory C1/C2, chỉ thêm dòng):** `internal/types/wiki_chess_ref.go` (+`ChessRefTypePosition`/`ChessRefSourcePosition`), `internal/application/service/wiki_page.go` (+1 dòng `chessRefPrefixes` — thiếu dòng này thì `[[position/x]]` bị coi là link wiki thường, lỗi CÂM), `internal/types/wiki_page.go` (+`WikiNodeTypeChessPosition` + nhánh `ChessRefTypeToNodeType`), `internal/handler/chess_ref.go` (+nhánh `SearchRefs`), `internal/application/repository/wiki_chess_ref.go` (+`ReplaceForPosition`/`DeleteForPosition` mẫu `ReplaceForLesson` — xóa theo `tenant_id+page_slug`, KHÔNG theo `kb_id`; +khối query thứ 3 trong `ListBacklinks`), `internal/application/service/agent_service.go` (+1 case đăng ký tool), `internal/agent/tools/definitions.go` (+hằng tên/nhãn/`ChessToolNames`), `config/builtin_agents.yaml` (+tool + 1 dòng prompt), `internal/application/service/chess_knowledge_indexer.go`/`chess_knowledge_text.go` (+`IndexPosition`/`buildPositionKnowledgeText`), `internal/types/chess_kb_index.go` (+`PositionsTotal`).
- **Frontend dùng chung bị chạm:** `utils/chessRef.ts` + `utils/chessBlocks.ts` (union `ChessRefType`/`CHESS_REF_TYPES` +`position`), `api/chess/index.ts` (+nhóm hàm `*Position*`), `components/{ChessWikiLinkSuggest,ChessRefMissing,ChessRefEmbed}.vue` (+nhãn/icon/màu), **`components/ChessBacklinks.vue`** (sửa bug có sẵn: nhị phân `lesson`/*else* khiến backlink `position` từng rơi nhầm route KB rỗng — nay 3 nhánh rõ ràng), `views/knowledge/wiki/WikiBrowser.vue` (+màu/nhãn/`nodeColorMap`/`isChessNodeType` — thiếu dòng cuối thì node đồ thị không click mở được), `views/chat/components/tool-results/ChessBoardDisplay.vue` (+`defineExpose({currentFen, currentIndex, currentLabel})` để `GameLibrary.vue` "kéo" FEN đang xem khi trích thế cờ từ ván — **không** dùng `emit` để tránh state trùng), `views/chess/{ChessManage,ChessCourses,GameLibrary}.vue` (+tab/picker/nút "Lưu thế cờ này"), `i18n/locales/vi-VN.ts` (đổi nhãn `type_puzzle`: "Thế cờ" → **"Bài tập"**, dành "Thế cờ" cho `position` — CHỈ đổi nhãn hiển thị, KHÔNG đổi slug/dữ liệu cũ).
- **Không cần migration** cho `wiki_chess_refs`/`chess_slug_aliases`/`chess_kb_index` — cột `chess_type`/`source_type` là `VARCHAR(16)` tự do, không CHECK/enum.
- **Test mới:** `internal/chess/fen_test.go`, `chess_library_position_test.go` (fake repo kiểu embed-interface như `chess_knowledge_indexer_test.go`; phủ slug unique/rename ghi alias/resolve exact→alias→fuzzy), `chess_lookup_position_test.go` (mẫu `chess_generate_puzzle_test.go`), + case mới trong `chess_knowledge_text_test.go`. `gofmt`/`go vet`/`go build ./...` sạch; `vue-tsc --noEmit` sạch (0 lỗi).
- **Môi trường dev (Windows, ghi lại để đỡ mất công lần sau):** `go build ./...` lỗi cgo `sqlite3.h: No such file` do thiếu `CGO_CFLAGS` trỏ vào `build_sqliteshim/` (header đã có sẵn trong repo, chỉ thiếu biến môi trường) — build với `CGO_CFLAGS="-I$(pwd)/build_sqliteshim"` thì qua. Không phải lỗi do lớp cờ.

### Thư viện sách cờ vua — Kệ → Sách → Chương (2026-08-10) — nhánh `feat/chess-book-library`
Thêm thực thể cờ THỨ 6 `book`/`chapter` (bên cạnh game/puzzle/lesson/course/position), cộng `shelf` (KHÔNG phải wikilink, chỉ điều hướng UI). Là hệ **SOẠN THẢO nội bộ** (markdown + FEN + ván cờ + ảnh) — KHÔNG phải kho ebook PDF/EPUB để đọc; khác hẳn BookStack Thầy đang chạy tại `D:\code\BookStack_chess` (nội dung "STEP TRAINNER" — repo đó KHÔNG được đụng, không nhập dữ liệu từ đó, Thầy chọn "bắt đầu mới hoàn toàn"). Kệ↔sách **nhiều-nhiều** (bảng nối `chess_shelf_books`, mẫu `bookshelves_books` của BookStack — 1 sách nằm nhiều kệ). Chương là danh sách 1 CẤP; cột `part` chỉ là nhãn chuỗi gộp hiển thị theo "Phần", không phải bảng riêng. Chỉ sách `status="published"` được index vào KB tri thức cờ (draft không rò vào câu trả lời agent).

**Quyết định kiến trúc:** sách/kệ/chương là "ngăn" thứ tư của `chessLibraryService`/`chessLibraryRepository`/`ChessLibraryHandler` (file mới `chess_library_book.go`, `chess_book.go` repo/handler — method trên CÙNG struct), **không phải service riêng** → `container.go` **0 dòng thay đổi** (trừ 1 tham số `fileService interfaces.FileService` thêm vào `NewChessLibraryService` — dig tự nối vì `interfaces.FileService` đã được `Provide` sẵn cho `initFileService`, không cần sửa container.go). `router.go` chỉ thêm 3 nhóm route (`/chess/shelves`, `/chess/books`, `/chess/chapters`) trong `RegisterChessLibraryRoutes` đã có.

- **Backend mới (file riêng, an toàn merge):** migration `000071_chess_books` (mục B, 6 bảng — xem chi tiết ở đó); `internal/types/chess_book.go` (`ChessShelf`/`ChessBook`/`ChessShelfBook`/`ChessBookChapter`/`ChessBookImage`/`ChessChapterRevision` + filter + bundle export/import); `internal/application/repository/chess_book.go`; `internal/application/service/chess_library_book.go` (kệ/sách/chương/phiên bản/ảnh — ~700 dòng, theo đúng khuôn 5 bước của position: Create sinh slug → GetBySlug exact→alias→fuzzy → Rename ghi alias → Delete cascade+dọn ref+gỡ index → sync ref); `internal/handler/chess_book.go`; `internal/application/service/chess_library_book_test.go` (fake repo kiểu embed-interface, mẫu `chess_library_position_test.go` — phủ slug unique, rename ghi alias, ràng buộc chương cần sách tồn tại, revision CHỈ tạo khi title/content đổi, khôi phục revision tạo thêm 1 bản mới, cascade xóa sách dọn sạch chương+lịch sử, kệ nhiều-nhiều).
- **Lịch sử phiên bản chương (tính năng MỚI so với mọi thực thể cờ khác):** `chess_chapter_revisions` lưu PRE-IMAGE (bản TRƯỚC khi ghi đè) — CHỈ tạo khi `Title`/`Content` thực sự đổi (sửa `sort_order`/`part` không tạo rác lịch sử). `RestoreChapterRevision` tái dùng `UpdateChapter` (khôi phục CŨNG tạo thêm 1 bản mới, lưu lại trạng thái trước khi khôi phục) — không có đường ghi riêng, giảm nguy cơ lệch logic.
- **Ảnh chèn trong chương — hạ tầng MỚI hoàn toàn** (WeKnora không có endpoint upload ảnh lẫn bảng asset nào trước đây): `POST /chess/books/:id/images` (multipart, giới hạn 10MB khớp `session/image_upload.go`, chỉ nhận png/jpeg/gif/webp) lưu qua `interfaces.FileService.SaveBytes` (provider theo tenant, path `book-images/<uuid><ext>`) + ghi `chess_book_images`; `GET /chess/books/images/:imageId` stream qua URL **ổn định** (KHÔNG dùng presigned URL có hạn dùng — nội dung markdown lưu URL này lâu dài). Xóa sách cascade xóa cả file vật lý (`FileService.DeleteFile`) lẫn bản ghi.
- **RAG có điều kiện theo Status:** `ChessKnowledgeIndexer.IndexBook`/`IndexChapter` (mới) chỉ chạy khi `book.Status == "published"`; đổi status draft↔published qua `UpdateBook`/`RenameBookSlug` tự động index/gỡ index lại toàn bộ sách + chương con (`reindexBookAndChapters`). `ReindexAll` thêm khối books/chapters (chỉ liệt kê sách published).
- **File dùng chung bị chạm (đã có sẵn trong inventory C1/C2, chỉ thêm dòng):** `internal/types/wiki_chess_ref.go` (+`ChessRefTypeBook`/`ChessRefTypeChapter`/`ChessRefSourceChapter` — sách KHÔNG phát wikilink, chỉ chương), `internal/application/service/wiki_page.go` (+2 dòng `chessRefPrefixes`), `internal/types/wiki_page.go` (+`WikiNodeTypeChessBook`/`...Chapter` + nhánh `ChessRefTypeToNodeType`), `internal/handler/chess_ref.go` (+2 nhánh `SearchRefs`), `internal/application/repository/wiki_chess_ref.go` (+`ReplaceForChapter`/`DeleteForChapter` mẫu `ReplaceForPosition`; +khối query thứ 4 trong `ListBacklinks`), `internal/application/service/chess_knowledge_indexer.go`/`chess_knowledge_text.go` (+`IndexBook`/`IndexChapter`/`buildBookKnowledgeText`/`buildChapterKnowledgeText`), `internal/types/chess_kb_index.go` (+`BooksTotal`/`ChaptersTotal`), `internal/handler/chess_library.go` (+2 field trong `ReindexKB`). **Không đụng** `agent_service.go`/`definitions.go`/`builtin_agents.yaml` — đợt này KHÔNG thêm tool agent mới (RAG qua `knowledge_search` sẵn có là đủ, giữ 7 tool cờ).
- **Frontend mới:** `views/chess/BookLibrary.vue` (~900 dòng — trang chính, khuôn `ChessCourses.vue`+`PositionBank.vue`: 2 cột sách/chương, toolbar lọc theo kệ/cấp độ/giai đoạn/trạng thái, dialog sách+chương, toolbar soạn thảo rút gọn tự viết — KHÔNG copy nguyên `manual-knowledge-editor.vue` — kèm dán/kéo-thả ảnh upload thẳng, picker chèn wikilink 5 tab), `views/chess/components/{ChessShelfManager,ChessChapterHistory}.vue` (dialog riêng), `views/chess/BookPrint.vue` (route độc lập `/book-print/:id`, KHÔNG lồng trong `/platform` để tránh sidebar/topbar — mở tab mới rồi Ctrl+P; repo không có generator PDF/DOCX server-side nên đây là hướng rẻ nhất), `utils/chessBookOptions.ts` (nhãn level/phase/status/shelf-kind).
- **Frontend dùng chung bị chạm:** `utils/chessRef.ts`+`utils/chessBlocks.ts` (`ChessRefType`/`CHESS_REF_TYPES` +`book`+`chapter`; `chessBlocks.ts` giờ `import type { ChessRefType } from './chessRef'` thay vì lặp lại union — dọn nợ nhỏ tiện thể), `api/chess/index.ts` (+nhóm hàm `*Shelf*`/`*Book*`/`*Chapter*`/`uploadBookImage`), `components/{ChessWikiLinkSuggest,ChessRefMissing,ChessRefEmbed,ChessBacklinks}.vue` (+nhãn/icon/màu cho book/chapter; `ChessBacklinks` +nhánh điều hướng nguồn `chapter`), `views/knowledge/wiki/WikiBrowser.vue` (+`nodeColorMap`/`isChessNodeType`/`getTypeTheme`/`getTypeLabel` cho `chess_book`/`chess_chapter`), `views/chess/ChessManage.vue` (+tab "Thư viện sách" thứ 5, lazy `v-if`, +nhánh `book`/`chapter` trong `watch(focusRef)`), `router/index.ts` (+route `chessBookPrint` **độc lập**, không lồng `/platform`), `i18n/locales/vi-VN.ts` (+`chess.ref.type_book`/`type_chapter`/`tabBooks`/`tabChapters` — **CHỈ vi-VN**: xác nhận namespace `chess.ref` chưa từng tồn tại ở en-US/ko-KR/ru-RU kể cả cho `position`, nên KHÔNG thêm nửa vời vào 3 locale kia, giữ đúng tiền lệ). **`utils/security.ts` (MỚI vào inventory, file dùng chung KHÔNG có "chess" trong tên):** `DOMPurifyConfig.ALLOWED_ATTR` +`data-chess-ref`/`data-chess-type` — bắt buộc vì `BookLibrary.vue` dùng `sanitizeHTML()` (khác `ChessCourses.vue`/`PositionBank.vue` dùng `marked.parse` trần) cho nội dung chương dài/nhiều nguồn; thiếu 2 attribute này thì DOMPurify âm thầm xóa data-attribute của chip `[[...]]`, click không mở được popup — lỗi CÂM, khó phát hiện qua test tay thông thường.
- **Không cần migration** cho `wiki_chess_refs`/`chess_slug_aliases`/`chess_kb_index` — cột `chess_type`/`source_type` là `VARCHAR(16)` tự do, không CHECK/enum.
- **Kiểm chứng:** `gofmt`/`go vet`/`go build ./...` sạch trên mọi file cờ mới+sửa. `npm run build` (vite) sạch, không lỗi. `npm run type-check` (`vue-tsc --build`) giữ nguyên **111 lỗi trước/sau** — xác nhận toàn bộ là nợ type upstream có sẵn (đối chiếu bằng `git stash -u` chạy lại), không phải do đợt này; **không** bật lại gate type-check trong CI (vẫn tắt theo quyết định WS-C cũ). `go test` KHÔNG chạy được trên máy Windows này (xem ghi chú môi trường bên dưới) — đã xác minh đúng đắn qua `go vet` (biên dịch test sạch) thay vì chạy thật; **cần chạy test này trên CI/Linux** để có xác nhận runtime đầy đủ trước khi coi là "đã test".
- **Môi trường dev (Windows, bổ sung phát hiện mới — đỡ mất công lần sau):** `go test` (không phải `go build`) trên máy này **CRASH SIGSEGV ngay tại `init()`** của package `internal/types` — cụ thể `github.com/yanyiwu/gojieba` (thư viện tách từ tiếng Trung, native cgo DLL) gọi `NewJieba()` lỗi truy cập bộ nhớ ngay khi nạp package, xảy ra cho **MỌI** test đụng transitively tới `internal/types` (tức gần như mọi gói), **kể cả trên nhánh gốc chưa đụng gì** (đã xác nhận qua `git stash -u` rồi chạy lại). Không phải lỗi do lớp cờ hay đợt sửa này. `go vet` KHÔNG bị ảnh hưởng (không link/chạy binary, chỉ biên dịch) — dùng `go vet ./...` để xác nhận test biên dịch đúng khi không chạy được test thật trên Windows.

**Ngoài phạm vi đợt này (backlog mới):**
- [ ] Nhập nội dung từ BookStack (Markdown/Portable ZIP `D:\code\BookStack_chess`) — Thầy chọn bắt đầu mới, không nhập.
- [ ] Xuất PDF/DOCX server-side thật (hiện dùng `BookPrint.vue` + Ctrl+P; repo chưa có thư viện generator).
- [ ] Chia sẻ sách công khai cho học viên/phụ huynh (token/public flag) — hiện chỉ RBAC nội bộ Viewer/Contributor.
- [ ] Wikilink `[[shelf/...]]` (kệ hiện chỉ điều hướng UI, không phải đích wikilink).
- [ ] Template chương mặc định theo sách (kiểu `default_template_id` của BookStack).
- [ ] Đổi slug cho course/lesson (nợ cũ, không phải phạm vi đợt sách).

### Move-list PGN chuẩn + in sách theo chương chọn lọc (2026-08-11)
Ba đợt sửa nhỏ liên tiếp trên cùng vùng "bàn cờ tương tác + in sách", commit thẳng `main` (không qua nhánh riêng — repo không bật branch protection, CI/CD `cicd-deploy.yml` tự build+test+deploy khi push `main`, xem `docs/deploy/cicd.md`).
- **Move-list gộp cặp Trắng/Đen:** `ChessBoardDisplay.vue` (dùng chung mọi nơi hiển thị bàn cờ) trước đây lặp số nước 2 lần (`1. d4 1... e6`) do lặp phẳng theo nửa-nước, không gom cặp. Thêm computed `moveGroups` gom theo `moveNumber`; tách `san` (hiện trong move-list) khỏi `label` đầy đủ (giữ nguyên cho thanh nav + dialog "Lưu thế cờ này" ở `GameLibrary.vue`). Số nước đọc từ **trường thứ 6 của FEN** (`fullmove number`) thay vì `Math.floor(i/2)+1` — công thức cũ giả định Trắng luôn đi trước, sai với ván bắt đầu từ thế cờ giữa ván (`[SetUp "1"][FEN "... b ..."]`). Đồng bộ backend: `internal/chess/fen.go` thêm `FullMoveNumber(fen)`, `internal/chess/board.go` (`ParsePGN`) dùng hàm này thay `i/2+1` — ảnh hưởng payload `plies` của tool `chess_evaluate_game`. Test mới: `board_test.go` (case PGN có `SetUp`/`FEN` bắt đầu từ nước 24, Đen đi trước).
- **`BookPrint.vue` hiện được move-list khi in:** trước đây `.move-list` nằm LỒNG trong `.chess-nav`, nên luật CSS ẩn nút điều hướng (`:deep(.chess-nav){display:none}` — vô nghĩa trên giấy) VÔ TÌNH ẩn luôn move-list — chương nhúng PGN in ra chỉ thấy hình bàn cờ, mất hẳn chuỗi nước đi dạng chữ. Sửa: tách `.move-list` thành khối `v-if` riêng, sibling của `.chess-nav` (không còn lồng trong nhau) trong `ChessBoardDisplay.vue`; `BookPrint.vue` thêm override bỏ `max-height`/`overflow` (vô nghĩa khi không cuộn được) + tắt cursor/hover.
- **In sách theo chương chọn lọc:** `BookPrint.vue` tách `chapters` (toàn bộ, nguồn cho dialog) khỏi `printedChapters` (theo `selectedIds`); đọc/ghi lựa chọn qua query `?chapters=id1,id2` (id KHÔNG dùng slug — `ChessBookChapter.slug` là optional); dialog "Chọn chương" khuôn giống `ChessShelfManager.vue` (t-dialog + ô tìm + list t-checkbox + confirm, chặn xác nhận khi 0 chương được tích); thêm tùy chọn "phần đầu sách" 3 mức (Đầy đủ/Gọn/Không, nhớ qua `localStorage` khoá `weknora_book_print_header_level`, cùng khuôn `COLUMNS_STORAGE_KEY` sẵn có). `BookLibrary.vue` thêm nút in nhanh trên từng chương (`openPrintChapter`, tái dùng khuôn `openPrintView`/`router.resolve`, chỉ thêm `query: { chapters: ch.id }`).
- **Tiện thể phát hiện + sửa (không liên quan đợt này):** `BookLibrary.vue` có sẵn 1 byte NUL lạ ở biến sentinel `lastPart` trong `buildBookMarkdown` (xác nhận qua `git show HEAD` — đã tồn tại từ trước, không phải do đợt sửa hôm nay) — thay về đúng dấu cách `' '` như ý định ban đầu; không đổi hành vi (byte NUL hay dấu cách đều phân biệt được với `part` thật).
- **Kiểm chứng:** `gofmt`/`go vet ./internal/chess/...`/`go test ./internal/chess/...` sạch (test chạy được — package `internal/chess` không kéo `internal/types` nên không dính SIGSEGV gojieba của môi trường Windows). `npm run build` (vite) sạch. `vue-tsc --build --force` giữ nguyên **111 lỗi** nợ upstream (không thêm lỗi mới, không có lỗi trong các file đã sửa).
- **Demo:** `scripts/seed_chess_book_print_demo.mjs` (mẫu `seed_chess_wikilink_demo.mjs`) — tạo 1 sách `status="draft"` (không lọt vào KB RAG) + 1 chương nhúng khối ` ```chess ` chứa PGN (ván Opera, Morphy 1858) để kiểm thử tay tính năng in.

### Hiển thị đầy đủ chú giải PGN — bình luận + NAG + nhánh phụ (2026-08-11)
Thầy soạn chương sách nhúng PGN có chú giải đầy đủ (bình luận `{...}`, ký hiệu đánh giá NAG `$1`/`$14`, nhánh phụ RAV `(7. O-O O-O 8. a3)`) — lưu rồi xem/in chỉ còn trơ nước đi trần, mất sạch. Nguyên nhân: `ChessBoardDisplay.vue` dùng `chess.js` `loadPgn()` + `history()` — `loadPgn` chỉ đi theo `node.variations[0]` và chỉ đọc `node.comment`; **NAG, suffix `!?`, và TOÀN BỘ nhánh phụ bị parse rồi vứt bỏ ÂM THẦM, không throw**.

- **Không thêm thư viện PGN mới.** `chess.js@1.4.0` (đã cài) ship kèm `node_modules/chess.js/src/pgn.js` export `parse()` — **chính là hàm `loadPgn()` gọi bên trong** (`chess.ts` ~dòng 2159), trả cây đầy đủ `move/suffix/nag/comment/variations`, hỗ trợ nhánh lồng nhiều tầng. `package.json` của chess.js KHÔNG có `exports` field nên deep-import `chess.js/src/pgn.js` hợp lệ cả với Vite (`moduleResolution: "bundler"`) lẫn `node --test` (Node 22+ tự phát hiện cú pháp ESM khi thiếu `"type"` trong package.json — đã xác minh chạy thật).
  ⚠️ **Rủi ro khi nâng cấp chess.js:** đường import này KHÔNG phải API công khai (không nằm trong `dist/`, không khai trong `exports`). Nếu nâng version mà build báo lỗi "Cannot find module 'chess.js/src/pgn.js'" — kiểm tra lại cấu trúc `src/` của gói mới trước khi coi là bug ở phía mình.
- **File mới (thuần, tách khỏi component để test được):** `frontend/src/utils/pgnAnnotated.ts` (`buildAnnotatedPgn`, `glyphOf`, `cleanComment`, `tokensFromPositions` + type `PgnToken`/`AnnotatedPos`/`NagSource`) + `pgnAnnotated.test.ts` (13 case, `node --test`, dùng PGN thật của Thầy làm ca chính). Cạm bẫy đã xác minh bằng chạy thật (không suy đoán): kiểu shipped `src/node.ts` khai `suffix?: string; nag?: string` nhưng **runtime trả MẢNG**; RAV của nước X nằm ở `variations[1..]` của node **cha** X (không phải của X); grammar throw với 2 comment liên tiếp / ngoặc nhọn lồng / NAG đặt sau comment — cả 3 đều là lỗi comment nên fallback tầng 2 là "xoá comment rồi parse lại" (giữ nước+NAG+nhánh phụ), KHÔNG phải `loadPgn()` (nó gọi chính `parse()` nên throw trên cùng input, vô nghĩa làm fallback).
- **`ChessBoardDisplay.vue`:** bỏ hẳn `moveGroups` (đợt sửa move-list trước), thay bằng MỘT `computed` (`model`) sinh cả `positions` (mainline, giữ NGUYÊN ngữ nghĩa `currentIndex`/`source_ply` mà `GameLibrary.vue` phụ thuộc) lẫn `tokens` (toàn bộ, gồm bình luận/nhánh phụ) — một bộ render duy nhất cho cả 3 nguồn dữ liệu (`plies` từ backend, PGN có chú giải, PGN trần) qua `tokensFromPositions()`.
- **Quyết định phạm vi đã chốt với Thầy:** nhánh phụ hiện dạng **CHỮ TĨNH, không bấm tua bàn cờ được** (tránh đụng state điều hướng `currentIndex`/`currentFen` ngoài mainline — rủi ro hồi quy trên tính năng đã ship). Dữ liệu máy `[%evp ...]`/`[%clk ...]` lẫn trong bình luận bị lọc, chỉ giữ chữ.
- **Cạm bẫy vue-tsc phát hiện khi làm (đáng nhớ cho lần sau):** thu hẹp kiểu (type narrowing) discriminated union **KHÔNG hoạt động qua NHIỀU tầng `v-if`/`v-else-if`/`v-else` nối tiếp trong template** — chỉ nhánh đầu tiên được loại đúng, các nhánh `v-else-if` sau vẫn giữ nguyên union đầy đủ (xác nhận bằng chạy `vue-tsc` thật, 111→126 lỗi khi thử, không phải suy đoán). Sửa bằng cách làm PHẲNG dữ liệu (`DisplayToken`, không union) TRƯỚC khi vào template thay vì ép kiểu (`as`) trong template.
- **`BookPrint.vue`:** đổi `:deep(.chess-board-display){break-inside:avoid}` → `auto`, chuyển `avoid` xuống `.chess-body` — bình luận dài giờ có thể làm khối bàn cờ cao hơn 1 cột, giữ `avoid` ở cấp cả khối sẽ đẩy nguyên khối sang cột sau y hệt bẫy đã biết ở cấp chương (`.bkp-chapter`, xem entry "in sách theo chương chọn lọc" phía trên). Thêm ép `#000` cho `.move-comment`/`.move-item`/`.move-no`/`.move-nag`/`.move-paren`/`.move-var` trong `@media print` (chế độ tối sẽ in chữ trắng nếu không ép).
- **Kiểm chứng:** `npm test` (`node --test`) 122/122 pass (109 cũ + 13 mới). `npm run build` (vite) sạch — cũng là phép thử thật Vite resolve được deep-import. `vue-tsc --build --force` giữ nguyên **111 lỗi** nợ upstream sau khi sửa xong lỗi narrowing (ban đầu tăng lên 126, đã truy và sửa hết — xem cạm bẫy ở trên).
- **HỒI QUY tự gây ra + đã sửa ngay sau đó (bài học quan trọng nhất đợt này):** đổi `.move-list` từ `display:flex; flex-wrap:wrap` sang `display:block` làm **mất hết điểm ngắt dòng** của chuỗi nước đi → khi in, cả đoạn `7. a3 (7. O-O O-O 8. a3) 7... O-O 8. O-O Be6 9. Bxe6 fxe6` thành MỘT dòng liền tràn khỏi cột, kéo theo trang in tràn NGANG khỏi khổ giấy nên trình duyệt sinh thêm 1 trang gần như trắng. **Nguyên nhân gốc:** trình biên dịch Vue (chế độ `whitespace: 'condense'` mặc định) **xoá sạch text node whitespace giữa các thẻ anh em** trong template — đã xác minh bằng cách biên dịch thật bằng `@vue/compiler-dom` (render function không hề có `" "` nào), không phải suy đoán. Với `flex` + `gap` thì không sao (flex tự xuống dòng bất kể whitespace), nhưng với `display:block` + inline span thì `margin` **KHÔNG** tạo điểm ngắt dòng ⇒ không chỗ nào bẻ được. **Sửa:** thêm hàm thuần `needsSpaceBefore(prev, cur)` trong `pgnAnnotated.ts` (test riêng) + render `<span class="move-sep">{{ ' ' }}</span>` — khoảng trắng THẬT, đồng thời bỏ khoảng trắng trước `)` và ngay sau `(` để nhánh phụ đọc đúng lối sách `(7. O-O O-O 8. a3)`. Bỏ luôn toàn bộ spacing bằng `margin-left` trước đó. **Ghi nhớ:** trong repo này, đổi bố cục move-list sang block/inline thì PHẢI tự chèn text node khoảng trắng; đừng trông vào whitespace trong template.
- **Lưới an toàn khi in (`BookPrint.vue`):** `:deep(.chess-board-display){max-width:100%}` + `:deep(.move-list){overflow-wrap:break-word}` + bỏ lề dưới ở phần tử cuối — chặn đúng cơ chế "tràn ngang → trình duyệt sinh trang thừa" ở trên, phòng cho cả nội dung lạ sau này.
- **Ngoài tầm CSS:** phần đầu/chân trang khi in (ngày giờ, tiêu đề, URL, số trang "1/2") là **tuỳ chọn của trình duyệt**, không phải do code — tắt ở hộp thoại In → *More settings* → bỏ tick *Headers and footers*.

### Ngân hàng bài viết — thực thể cờ thứ 7 (2026-08-11) — Phase 1
Thêm thực thể cờ THỨ 7 `article` (bên cạnh game/puzzle/lesson/course/position/book+chapter): trang tri thức ĐỘC LẬP về khái niệm/thuật ngữ/kinh nghiệm — KHÔNG thuộc sách/khóa học nào, đúng ontology "Concept" đã ghi ở `02-mien-co-vua.md` §2.5 nhưng trước đó chưa hiện thực hóa. Wikilink `[[article/<slug>]]`. Thiết kế đầy đủ (tổ chức 2 trục cây chuyên mục+lọc, bí danh qua `chess_slug_aliases.kind`, ảnh, lịch sử phiên bản, in ấn) đã chốt trong plan `t-i-mu-n-th-m-t-nh-squishy-pike.md`; **đợt này chỉ làm Phase 1 (xương sống): CRUD + wikilink 2 chiều + RAG**, CHƯA có chuyên mục/bí danh-ghi-DB/ảnh/lịch sử phiên bản/in ấn (xem backlog bên dưới).

- **Migration `000072_chess_articles`** (mục B) — tạo LUÔN cả 5 bảng cho toàn bộ tính năng (không chỉ Phase 1): `chess_articles`, `chess_article_topics` (cây chuyên mục tối đa 2 tầng qua `parent_id`, ép ở service — KHÔNG phải đích wikilink, giống `chess_shelves`), `chess_article_topic_items` (pivot nhiều-nhiều, mẫu `chess_shelf_books`), `chess_article_images` (sao y `chess_book_images`), `chess_article_revisions` (sao y `chess_chapter_revisions`) — 4 bảng sau CHƯA có code Go dùng tới (Phase 2/3). Cộng `ALTER TABLE chess_slug_aliases ADD COLUMN kind VARCHAR(16) DEFAULT 'rename'` (**sửa bảng CŨ, migration 000068** — lý do xem mục "Bí danh" bên dưới).
- **Backend mới (file riêng, an toàn merge, khuôn `chess_position.go`):** `internal/types/chess_article.go`; `internal/application/repository/chess_article.go` (method trên `chessLibraryRepository` sẵn có — CHỈ CRUD bài viết, chưa có topic/image/revision dù type đã khai báo); `internal/application/service/chess_library_article.go` (đúng khuôn 5 bước: Create→slug, GetBySlug exact→alias→fuzzy, RenameArticleSlug ghi alias, DeleteArticle cascade ref 2 chiều + gỡ index, `syncArticleChessRefs`; + `reindexArticle` đối xứng bắt buộc với `reindexBookAndChapters` — publish→index, draft→**gỡ**, để hạ cấp không để sót tri thức cũ trong KB); `internal/handler/chess_article.go`.
- **File dùng chung bị chạm (đã có sẵn trong inventory C1/C2, chỉ thêm dòng — KHÔNG đụng `agent_service.go`/`definitions.go`/`builtin_agents.yaml`, giữ 7 tool cờ, RAG đi qua `knowledge_search` sẵn có đúng tiền lệ sách):** `internal/types/wiki_chess_ref.go` (+`ChessRefTypeArticle`/`ChessRefSourceArticle` — bài viết vừa là đích vừa là nguồn), `internal/types/wiki_page.go` (+`WikiNodeTypeChessArticle`+nhánh `ChessRefTypeToNodeType`), `internal/types/chess_kb_index.go` (+`ArticlesTotal`), `internal/types/interfaces/chess_library.go` (+method Article vào cả `ChessLibraryService`+`ChessLibraryRepository`), `internal/application/service/wiki_page.go` (+1 dòng `chessRefPrefixes` — thiếu dòng này thì `[[article/x]]` bị coi là link wiki thường, lỗi CÂM), `internal/application/service/chess_slug.go` (+`articleSlugBase`), `internal/application/service/chess_library.go` (+khối articles trong `ReindexAll`, chỉ liệt kê bài `published`), `internal/application/service/chess_knowledge_text.go` (+`buildArticleKnowledgeText` — đưa cả `Aliases` vào văn bản index để RAG bắt trúng khi hỏi bằng từ đồng nghĩa), `internal/application/service/chess_knowledge_indexer.go` (+`IndexArticle`), `internal/application/repository/wiki_chess_ref.go` (+`ReplaceForArticle`/`DeleteForArticle` mẫu `ReplaceForPosition`; +khối query thứ 5 trong `ListBacklinks`), `internal/handler/chess_ref.go` (+nhánh `SearchRefs` dùng `SearchArticles`, khuôn `SearchChapters` — tìm cả content, không phải khuôn `Keyword` filter của position/book), `internal/router/router.go` (+nhóm route `/chess/articles`, static `export`/`import`/`by-slug` đặt TRƯỚC `:id`).
- **Bí danh/từ đồng nghĩa — phát hiện lỗ thật khi đọc code trước khi thiết kế:** `internal/application/repository/chess_slug_alias.go` lúc đó CHỈ có `ResolveAlias`+`AddAlias` (upsert), **KHÔNG có xóa** — nếu Thầy gõ bí danh "pin" rồi xóa khỏi ô, alias vẫn nằm lại DB vĩnh viễn (rác không gỡ được qua UI). Giải pháp đã chốt trong plan: thêm cột `kind` (`'rename'` = sinh do đổi slug, giữ vĩnh viễn; `'synonym'` = người dùng gõ, sửa/xóa tự do qua `ReplaceSynonyms`/`DeleteAliasesFor` MỚI) — **migration đã thêm cột `kind` (mục B) nhưng Phase 1 CHƯA thêm field `Kind` vào Go struct `ChessSlugAlias` lẫn 2 method mới** (để Phase 2). Vì Go struct chưa có field này, `AddAlias` hiện có (dùng cho article rename + 6 nơi gọi cũ) không set cột `kind` trong INSERT → cột nhận DEFAULT `'rename'` từ DB, đúng ý nghĩa, KHÔNG cần sửa code — xác nhận bằng cách đọc GORM: field không khai trong struct thì không có trong câu `INSERT`.
- **Frontend mới:** `frontend/src/views/chess/ArticleBank.vue` (~470 dòng — 2 khung: danh sách+lọc bên trái theo khuôn `PositionBank.vue`, khung phải Xem/Sửa **INLINE, không dùng modal** — quyết định cố ý vì bài viết là văn bản dài, modal hợp với chương ngắn của `BookLibrary.vue` hơn; luồng tạo: dialog chỉ hỏi Tiêu đề+Thể loại → lưu ngay `status=draft` → tự mở khung Sửa, để bài đã có `id` trước khi gõ chữ đầu — gỡ trước ràng buộc "phải lưu trước khi dán ảnh" mà `BookLibrary.vue` đang vướng, dù Phase 1 chưa có upload ảnh để tận dụng ngay); `frontend/src/utils/chessArticleOptions.ts` (khuôn `chessBookOptions.ts` — category/level/status options+label).
- **Frontend dùng chung bị chạm — nhóm "lỗi CÂM nếu quên" (đã rà đủ theo danh sách trong plan):** `frontend/src/utils/chessRef.ts` (`ChessRefType` union+`REF_TYPES`+nhánh `doResolve` chép `book`: `board:null`+`BACKLINK_FN`), **`frontend/src/utils/chessBlocks.ts`** (`CHESS_REF_TYPES` — chuỗi regex alternation KHÔNG suy ra từ `REF_TYPES`, quên là link render thành chữ thường không báo lỗi — đã thêm `|article`), `frontend/src/api/chess/index.ts` (+`ChessArticle`+nhóm hàm CRUD/export/import+`'article'` vào union `ChessRefSearchItem.type`), `components/ChessWikiLinkSuggest.vue`+`ChessRefMissing.vue` (2 bản `TYPE_LABELS` riêng+màu `[data-type]` — `#c41d7f`), `components/ChessRefEmbed.vue` (`iconMap` Record vét cạn — icon `article-1`), **`components/ChessBacklinks.vue`** (nhánh `iconFor`/`classFor`/`open` cho `article` — bắt buộc vì bài viết là NGUỒN backlink, thiếu thì backlink từ bài viết rơi vào nhánh wiki thiếu `kb_id` → mở KB rỗng, đúng bug đã từng xảy ra với `position`), `views/chess/ChessManage.vue` (tab thứ 6 lazy `v-if`+`focusArticleSlug`+nhánh `watch(focusRef)`), `views/knowledge/wiki/WikiBrowser.vue` (`nodeColorMap`/`isChessNodeType`/`getTypeTheme`/`getTypeLabel` +`chess_article`; **tiện thể sửa** `navigateToSlug` L3278 — regex trước đó `/^(game|puzzle|lesson|course)\//` đã thiếu sẵn `position|book|chapter`, bổ sung cả 4 cùng lúc với `article`), `i18n/locales/vi-VN.ts` (+`chess.ref.type_article`/`tabArticles` — CHỈ vi-VN, giữ đúng tiền lệ vì 3 locale kia chưa từng có namespace `chess.ref`), `views/chess/BookLibrary.vue`+`ChessCourses.vue` (picker wikilink 2 nơi: thêm tab "Bài viết" + `Promise.all` gọi `listArticles()` + nhánh `pickerItems`, để soạn chương sách/bài giảng chèn được `[[article/...]]` bằng chuột).
- **Kiểm chứng:** `gofmt -l` sạch trên mọi file đã sửa/thêm; `go build ./...` (với `CGO_CFLAGS`) sạch; `go vet ./...` — 1 lỗi biên dịch test tại `internal/datasource/scheduler_test.go` xác nhận **hoàn toàn không liên quan** (fake repo thiếu `UpdateSyncState`, package chưa từng bị đợt này đụng tới — `git diff --stat` xác nhận 0 thay đổi); `go vet` scope hẹp vào mọi package đã sửa (`types`, `application/repository`, `application/service`, `handler`, `router`, `chess`, `agent/tools`, `container`) sạch tuyệt đối. `npm run build` (vite) sạch. `npm test` (`node --test`) 123/123 pass, không case nào hỏng. `npm run type-check` (vue-tsc) giữ nguyên **111 lỗi** nợ upstream — đã lọc riêng, xác nhận 0 lỗi rơi vào bất kỳ file nào của đợt này.
### Ngân hàng bài viết — Phase 2: chuyên mục + bí danh thật + ảnh (2026-08-11)
Hoàn thiện 3 mảnh còn thiếu của Phase 1 theo đúng thiết kế trong plan.

- **Chuyên mục (`chess_article_topics`):** repo `chess_article_topic.go` + service `chess_library_article_topic.go` mới, method trên struct `chessLibraryRepository`/`chessLibraryService` sẵn có (0 thay đổi `container.go`). Ràng buộc **2 tầng ép ở SERVICE** (không CHECK ở DB): `validateTopicParent` chặn gán cha nếu bản thân cha không phải chuyên mục GỐC; `DeleteArticleTopic` chặn xóa nếu còn con. Gán bài viết vào chuyên mục làm **TỪ PHÍA CHUYÊN MỤC** (`SetTopicArticles`, chọn nhiều bài/1 lần) — cùng khuôn `SetShelfBooks`/`ChessShelfManager.vue`, đơn giản hơn hẳn gán từ phía bài viết (khỏi diff nhiều chuyên mục cùng lúc). Routes `/chess/article-topics` (+ `PUT /:id/articles`). Frontend: `ChessArticleTopicManager.vue` (chép khuôn `ChessShelfManager.vue`) + khung trái 220px trong `ArticleBank.vue` (cây gốc/con, click lọc qua `filter.topic_id` — cơ chế lọc đã có sẵn từ Phase 1 `ChessArticleFilter.TopicID`).
- **Bí danh thật:** thêm `Kind string` vào `types.ChessSlugAlias` (cột `kind` đã có sẵn từ migration 000072 Phase 1, chỉ giờ mới khai trong Go struct) + `ReplaceSynonyms`/`DeleteAliasesFor` trong `chess_slug_alias.go` (cả repo lẫn interface). `AddAlias` nay set tường minh `Kind:"rename"` (trước đó ngầm định qua DB DEFAULT). **Phát hiện khi cài `syncArticleAliases`:** unique key của bảng alias là `(tenant_id, chess_type, old_slug)` — KHÔNG phân biệt theo `kind` — nên gọi `ReplaceSynonyms` lại với CÙNG danh sách bí danh nhưng `targetSlug` MỚI (sau khi đổi slug bài viết) sẽ tự UPDATE đúng `new_slug` của các dòng cũ qua `ON CONFLICT (old_slug)` thay vì để mồ côi — xác nhận bằng cách truy vết logic UPSERT, không phải đoán; nhờ vậy `RenameArticleSlug` chỉ cần gọi lại `syncArticleAliases(ctx, updated)` là bí danh tự trỏ theo slug mới, không cần code riêng. Va chạm: `syncArticleAliases` bỏ qua (không lưu) bí danh trùng `ArticleSlugExists` của MỘT BÀI KHÁC — không có toast báo cho người dùng (đơn giản hóa có chủ đích, xem giới hạn dưới). `DeleteArticle` gọi `DeleteAliasesFor` dọn sạch mọi alias (cả rename lẫn synonym) khi xóa hẳn bài.
  - **Giới hạn CHƯA giải quyết (biết trước, không phải bug mới):** resolve alias là **1 hop, không đệ quy** — nếu một bí danh synonym vô tình trùng CHỮ với một slug đã bị đổi tên (rename) trước đó của MỘT bài khác, việc ghi đè phụ thuộc thứ tự gọi UPSERT (ai ghi sau thắng). Đây là đặc tính có sẵn của toàn hệ alias (game/puzzle/position/book/chapter đổi slug 2 lần cũng có cùng giới hạn 1-hop), KHÔNG phải riêng bug của bài viết — không sửa trong đợt này vì là thay đổi cross-cutting ngoài phạm vi.
- **Ảnh chèn bài (`chess_article_images`):** repo+service+handler sao y `chess_book.go`/`chess_library_book.go` phần ảnh (đổi `book_id`→`article_id`, tái dùng nguyên `maxBookImageSize`/`bookImageExtByMime` đã có — không định nghĩa lại). Routes `POST /chess/articles/:id/images` + `GET /chess/articles/images/:imageId` (static TRƯỚC `:id`). `DeleteArticle` cascade xóa ảnh (bản ghi + file vật lý qua `FileService.DeleteFile`).
  - **Frontend — `useChessEditor.ts` MỚI** (composable thuần: chèn tại con trỏ, bọc lựa chọn, khối ` ```chess `, dán/kéo-thả ảnh) — CHỈ `ArticleBank.vue` dùng, **KHÔNG refactor** phần soạn thảo nội bộ của `BookLibrary.vue` (đúng quyết định đã ghi trong plan — tránh rủi ro hồi quy trên file vừa ship). Toolbar rút gọn (đậm/nghiêng/tiêu đề/danh sách/khối cờ/ảnh) + input file ẩn, gắn qua `editor.bindTextarea()` trong cùng `watch(editing)` đã dùng để bind `ChessWikiLinkSuggest`.
- **Kiểm chứng:** `gofmt -l` sạch mọi file sửa/thêm (trừ `wiki_page.go` — lỗi nợ sẵn không liên quan, đã xác nhận từ Phase 1); `go build ./...` sạch; `go vet` sạch trên mọi package đã sửa. `npm run build` sạch. `npm test` 123/123 pass. `npm run type-check` giữ nguyên **111 lỗi**, lọc riêng xác nhận 0 lỗi rơi vào file đợt này.
### Ngân hàng bài viết — Phase 3: lịch sử phiên bản (2026-08-11)
Sao y cơ chế `chess_chapter_revisions` cho bài viết (`chess_article_revisions`, bảng đã có sẵn từ migration 000072 Phase 1) — cùng khuôn `UpdateChapter`/`ChessChapterHistory.vue`.

- **Backend:** repo (`CreateArticleRevision`/`ListArticleRevisions`/`GetArticleRevision`/`CountArticleRevisions`/`DeleteArticleRevisionsByArticle`, append vào `chess_article.go`) + service. **Đổi chữ ký `UpdateArticle`** (Service interface lẫn implementation) thêm tham số `revisionNote string` — lưu bản PRE-IMAGE (Title/Content TRƯỚC khi ghi đè) CHỈ khi Title/Content thực sự đổi (tránh rác lịch sử khi chỉ sửa category/level/status/tags). `RestoreArticleRevision` tái dùng `UpdateArticle` (khôi phục CŨNG tạo thêm 1 bản mới) — không có đường ghi riêng. `DeleteArticle` cascade dọn `DeleteArticleRevisionsByArticle`. Routes lồng trong CÙNG group `/chess/articles` (không tách group riêng như `/chess/chapters` — vì chương cần tách do book/chapter là 2 namespace `:id` khác nhau, còn revision bài viết vẫn nằm trong CÙNG namespace `:id` của chính bài viết, không xung đột wildcard).
  - **Đặt tên tránh nhầm:** tham số ghi chú thay đổi đặt tên `revisionNote`/JSON `revision_note` — KHÔNG dùng lại tên `summary`, vì `ChessArticle.Summary` đã là một trường PERSISTED khác hẳn (tóm tắt ngắn hiển thị ở danh sách/RAG), khác bản chất với `ChessArticleRevision.Summary` (ghi chú thay đổi của MỘT lần sửa, ephemeral). Chapter không có vấn đề này vì `ChessBookChapter` không có trường `Summary` riêng.
- **Frontend:** `ChessArticleHistory.vue` (chép nguyên khuôn `ChessChapterHistory.vue`) + nút "Lịch sử" trong header khung xem (khi KHÔNG ở chế độ sửa) + ô "Ghi chú thay đổi (tùy chọn)" trong form sửa, gửi kèm `revision_note` khi lưu.
- **RAG (Phase 1, xác nhận lại — không có code mới đợt này):** `reindexArticle`/`IndexArticle`/khối `articles` trong `ReindexAll` đã hoàn chỉnh từ Phase 1. Việc "chạy thật" (bật `CHESS_KB_INDEX=true` trên production + gọi `POST /chess/library/reindex` + xác nhận qua `GET /chess/library/index-status`) là thao tác VẬN HÀNH trên server, ngoài phạm vi tác động của agent — Thầy làm theo `docs/chess-rag-enable.md` khi sẵn sàng.
- **Kiểm chứng:** `gofmt -l`/`go build`/`go vet` sạch (scope mọi package đã sửa). `npm run build` sạch. `npm test` 123/123 pass. `npm run type-check` giữ nguyên **111 lỗi**, lọc riêng xác nhận 0 lỗi rơi vào file đợt này.
### Ngân hàng bài viết — Phase 4: trang in + demo (2026-08-11) — HOÀN TẤT 4 PHA
`ArticlePrint.vue` route độc lập `/article-print/:id` (ngoài `/platform`, khuôn `chessBookPrint`) + nút "Trang in" trong `ArticleBank.vue` (`openPrintView`, khuôn `openPrintView` của `BookLibrary.vue`).

- **Rút gọn so với `BookPrint.vue`, không phải tách dùng chung:** bài viết là VĂN BẢN ĐƠN (không có khái niệm chương/phần) nên bỏ hẳn dialog "Chọn chương", tùy chọn "phần đầu sách", và `chapterGroups`/`groupByPart` — chỉ còn toggle 1/2 cột + nút in. Toàn bộ cơ chế in (khối `@page`, break rules cho `.chess-board-display`/`.move-list`, `@media print` ép màu đen, khối style KHÔNG scoped sửa `html/body/#app` chống trang trắng thừa) **CHÉP NGUYÊN** từ `BookPrint.vue` — không refactor tách chung, giữ đúng quyết định đã ghi trong plan (đụng vào CSS đã dò tay kỹ ở đợt trước để tiết kiệm dòng là đổi rủi ro hồi quy lấy code đẹp).
- **`scripts/seed_chess_article_demo.mjs`** (khuôn `seed_chess_book_print_demo.mjs`): tạo 1 bài "Ghim (Pin) là gì?" — có bí danh ("Pin, Đóng đinh"), khối ` ```chess ` minh họa thế cờ ghim tuyệt đối, và wikilink mẫu ra `[[article/xien|...]]` — dùng kiểm tra tay CRUD/resolve bí danh/wikilink/backlinks/in ấn trong một lượt.
- **Không có thay đổi backend** ở pha này (thuần frontend + script demo).
- **Kiểm chứng:** `npm run build` sạch. `npm test` 123/123 pass. `npm run type-check` giữ nguyên **111 lỗi**, lọc riêng xác nhận 0 lỗi rơi vào file đợt này.

**Tổng kết toàn bộ tính năng (Phase 1–4, PR #10/#11/#12/#13, đều đã merge vào `main`):** Ngân hàng bài viết là thực thể cờ thứ 7, đầy đủ CRUD + wikilink 2 chiều + chuyên mục 2 tầng + bí danh thật + ảnh chèn bài + lịch sử phiên bản + RAG (code xong, cần bật vận hành) + trang in + script demo. Backlog còn lại (không chặn dùng được, ưu tiên thấp):
- [ ] (Nợ, thấp ưu tiên, từ Phase 2) Toast báo "bí danh X bị bỏ qua vì trùng slug thật của bài khác" — hiện chỉ âm thầm bỏ qua ở backend, an toàn nhưng không thân thiện.
- [ ] (Nợ vận hành) Bật `CHESS_KB_INDEX` + reindex + xác nhận `index-status` trên production để RAG bài viết chạy thật — xem `docs/chess-rag-enable.md`.
- [ ] (Nợ, từ Phase 2) Chuyển `BookLibrary.vue` sang dùng `useChessEditor.ts` (gỡ trùng lặp với toolbar soạn thảo nội bộ của nó) — làm khi có thể chạy app kiểm thử runtime, tránh hồi quy.
- [ ] Wikilink `[[topic/...]]` cho chuyên mục bài viết (hiện chỉ điều hướng UI, như `shelf`).
- [ ] Export/Import UI cho bundle bài viết đã có API (`exportArticles`/`importArticles`, nút đã có trong `ArticleBank.vue` từ Phase 1) — chỉ cần kiểm thử tay round-trip thực tế.

### RAG bài viết: vá lỗ hổng "mù" + diệt tài liệu mồ côi (2026-08-14)
Thầy hỏi "Ngân hàng bài viết có được nạp vào Kho tri thức không?". Kiểm chứng code thật: **chuỗi index đã đầy đủ và ngang hàng sách từ Phase 1** (`IndexArticle` đủ 4 gate, hook mọi đường CRUD, `published→draft` gỡ khỏi KB, `ReindexAll` lọc `Status=published`, văn bản index có bí danh + tóm tắt + content đã expand wikilink). **Vấn đề thật là không có cách nào BIẾT nó đã chạy** — cộng 3 lỗ hổng khác lộ ra khi rà.

- **`articles_total` rơi mất khỏi API** (`handler/chess_library.go`): service tính đúng nhưng handler dựng `gin.H` **thủ công** (để giữ 2 khóa `games_indexed`/`puzzles_indexed` tương thích ngược) nên field thêm vào `ChessReindexResult` từ Phase 1 không bao giờ tới client. Đã thêm + **ghi comment cảnh báo tại chỗ**: mỗi lần thêm field vào struct PHẢI thêm một dòng ở handler, nếu không lặp lại đúng lỗi này.
- **`index-status` không tách theo loại** → không biết bài viết đã vào kho chưa. Thêm `CountByType` vào `ChessKBIndexRepository` (repo+interface) + field `ByType map[string]int64` vào `ChessIndexStatus`, `IndexStatus` điền best-effort. **Dùng map thay vì 7 field rời** để thêm loại thứ 8 sau này không phải sửa cả struct lẫn frontend. **Cạm bẫy đã xử lý:** `IndexStatus(ctx)` không nhận `tenantID` làm tham số → dùng `types.TenantIDFromContext` (bản KHÔNG panic, khác `MustTenantIDFromContext` của handler); không có tenant trong ctx thì bỏ qua phần đếm nhưng vẫn giữ `ByType` là map RỖNG (không nil) để JSON ra `{}` chứ không phải `null`. Có test riêng cho cả 2 nhánh.
- **Tài liệu mồ côi khi đổi slug — lỗi có sẵn cho CẢ 6 loại**, không riêng bài viết: `upsert` tra mapping theo slug MỚI, không thấy nên tạo document MỚI; document + mapping của slug CŨ nằm lại vĩnh viễn → kho có 2 bản cùng nội dung, agent có thể trích dẫn bản đã đổi tên. Sửa bằng helper dùng chung `removeStaleIndex(ctx, tenantID, chessType, oldSlug)` (đặt cạnh `pruneChessRefs` trong `chess_library.go`), gọi ngay trước lệnh index slug mới ở **đủ 6 hàm** `RenameGameSlug`/`RenamePuzzleSlug`/`RenamePositionSlug`/`RenameBookSlug`/`RenameChapterSlug`/`RenameArticleSlug` — cả 6 đều đã có sẵn biến `oldSlug` cho alias nên chỉ là chèn 1 dòng. **Không áp cho** `RenameShelfSlug`/`RenameArticleTopicSlug` (kệ/chuyên mục không phải đích wikilink, không bao giờ vào KB). Đổi slug sách chỉ gỡ slug sách: chương giữ slug riêng, không đổi theo sách.
- **Panel "Kho tri thức" (frontend MỚI):** trước đợt này `POST /reindex` và `GET /index-status` **không có giao diện nào** — `grep` toàn `frontend/src` ra 0 hit, chỉ gọi được bằng `curl`. Thêm `components/ChessKBStatusDialog.vue` + nút ở header `ChessManage.vue` (đặt ngoài mọi tab vì kho phục vụ cả 7 loại). Panel hiện 4 điều kiện tiên quyết dạng chip xanh/đỏ **kèm câu xử lý tiếng Việt ngay tại chỗ** (lấy từ bảng chẩn đoán §4b runbook — giữ đồng bộ khi sửa runbook), bảng `by_type`, tiến độ embedding, nút Đẩy lại index + Làm mới. Bắt 403 riêng → hiện "Cần quyền Contributor" thay vì lỗi đỏ (cả 2 endpoint đều `g.Contributor()`).
  - **Nhấn mạnh trong UI (chỗ từng hiểu nhầm ở đợt RAG đầu):** "đã đẩy vào kho" (`by_type`) KHÁC "đã embed xong" (`completed`). `by_type.article=12` mà `pending=12` là agent CHƯA tìm thấy.
- **`config/builtin_agents.yaml` (C1, rủi ro merge cao):** system prompt `builtin-chess-coach` trước đó chỉ nhắc "lý thuyết, **sách**, **ván mẫu**" — **không hề nhắc bài viết**, nên model có thể không nghĩ tới việc tra kho khi được hỏi một khái niệm. Bổ sung đủ 7 loại vào câu mô tả + thêm một dòng hướng dẫn: hỏi "X là gì"/định nghĩa thuật ngữ/kinh nghiệm dạy → ƯU TIÊN `knowledge_search` trước. **Chỉ sửa system prompt**, KHÔNG đụng `allowed_tools`/`kb_selection_mode`/threshold (đã đúng sẵn: có `knowledge_search`+`grep_chunks`, `kb_selection_mode: all`, `retrieve_kb_only_when_mentioned: false`).
- **Test (trước đợt này bài viết có 0 test, sách có file 22KB):** thêm `chess_library_article_test.go` (fake repo embed-interface theo khuôn `chess_library_book_test.go`; fake alias repo theo dõi RIÊNG `renames` vs `synonyms` để khẳng định `ReplaceSynonyms` KHÔNG đụng lịch sử rename) phủ: slug duy nhất/va chạm, rename ghi alias + slug cũ còn resolve, bí danh được slugify, **bí danh trùng slug thật của bài khác bị bỏ qua**, đổi slug thì bí danh trỏ theo slug mới không mồ côi, revision chỉ tạo khi Title/Content đổi (sửa metadata KHÔNG tạo rác), restore tạo thêm bản mới, chặn chuyên mục tầng 3, chặn xóa chuyên mục còn con, cascade xóa (ref 2 chiều + alias + pivot + revision), index bỏ qua draft/không slug. Thêm test `by_type` (cả nhánh có và không có tenant trong ctx) + `buildArticleKnowledgeText` (bí danh vào text index, wikilink được expand, fallback title→slug). `stubIdxRepo` trong `chess_knowledge_indexer_test.go` được bổ sung `CountByType` + `newTestIndexerWithCounts`.
- **`docs/chess-rag-enable.md` cập nhật** (file viết 30/6, đã lệch thực tế): bảng "loại nội dung được index" (7 loại + quy tắc chỉ `published`), mục panel Kho tri thức làm cách chính (`curl` thành dự phòng), response reindex mẫu bản mới đủ 6 `*_total`, mục 3 đổi từ "THÊM `knowledge_search`/ĐỔI `kb_selection_mode`" sang "YAML repo ĐÃ cấu hình xong, chỉ cần VPS chạy đúng bản này", **thêm cạm bẫy "bản ghi đè DB làm YAML vô tác dụng"** (`GetAgentByID` ưu tiên `custom_agents`, YAML chỉ fallback — triệu chứng `tool not found: knowledge_search` dù YAML đủ; kèm SQL kiểm tra), giải thích `by_type` vs `completed`, mục 6 "sửa nội dung sau khi đã index", cảnh báo `Limit(500)` cắt âm thầm khi reindex.
- **Kiểm chứng:** `gofmt -l` sạch mọi file đợt này; `go build ./...` sạch; `go vet` sạch (types/application/handler/router). **`go test` vẫn CRASH SIGSEGV** tại `internal/types.init()` → `gojieba.NewJieba` (`evaluation.go:18`) trên máy Windows này — **đã xác nhận lại là lỗi môi trường**: chạy một test CŨ (`TestCreateBook_GeneratesSlug`, có từ trước) cũng crash y hệt. Test mới chỉ được xác nhận **biên dịch sạch** qua `go vet`; **phải chạy trên CI/Linux** mới coi là đã kiểm runtime. `npm run build` sạch; `npm test` **130/130 pass**; `npm run type-check` giữ nguyên **111 lỗi** nợ upstream, lọc riêng xác nhận 0 lỗi trong file đợt này.
- **Còn lại (vận hành, ngoài tầm agent):** bật `CHESS_KB_INDEX` trên VPS · chạy "Đẩy lại index" một lần · kiểm bảng `custom_agents` xem có bản ghi đè `builtin-chess-coach` không · nếu panel báo `searchable:false` thì xóa KB "Tri thức cờ vua" rồi đẩy lại index (code chỉ cảnh báo, không tự sửa).

### Đồng bộ mục lục sách với kho tri thức (2026-08-14) — nhánh `feat/chess-kb-toc-sync`
Thầy hỏi "Thư viện sách có được nạp vào Kho tri thức không?". Rà từng thao tác: **CÓ, dây chuyền chính hoàn chỉnh** (không có hàm viết ra rồi không ai gọi) — nhưng lộ ra 3 chỗ rò mà PR #14 chưa chạm tới. *(Bẫy vận hành số 1, KHÔNG phải bug: sách mặc định `draft`, `IndexBook` bỏ qua **hoàn toàn im lặng** → soạn xong mà quên bấm "xuất bản" thì kho rỗng, không log không lỗi.)*

- **Mục lục sách trong KB bị lệch — nghiêm trọng nhất.** `buildBookKnowledgeText` nhúng mục lục dựng từ `Title`/`Part`/**thứ tự** chương, nhưng **không thao tác nào trên chương nạp lại tài liệu SÁCH**: thêm chương → mục lục thiếu; đổi tiêu đề → mục lục còn tên cũ; xóa chương → mục lục vẫn còn; **`ReorderChapters` không gọi index gì cả** → mục lục sai thứ tự vĩnh viễn; `ImportBooks` gọi `CreateBook(chapters=nil)` → mục lục **rỗng** sau khi nhập. Sửa bằng helper **nhẹ** `reindexBookTOC` (`chess_library_book.go`) — nạp lại RIÊNG tài liệu sách, **cố ý KHÔNG** đụng tài liệu từng chương. **Đừng dùng `reindexBookAndChapters` cho việc này** (nó index lại MỌI chương — sách 50 chương mà sửa 1 chương thành 50 lần embed lại; hàm đó chỉ dành cho lúc `Status` đổi). Gọi ở `CreateChapter`/`DeleteChapter`/`RenameChapterSlug`/`ReorderChapters`; riêng `UpdateChapter` **chỉ gọi khi `Title`/`Part`/`SortOrder` đổi** — sửa nội dung là thao tác thường xuyên nhất và không đụng mục lục. `reindexBookTOC` mở đầu bằng `s.indexer.Enabled()` để khỏi tốn 2 truy vấn khi gate tắt (`indexer` là kiểu cụ thể `*ChessKnowledgeIndexer` nên gọi trực tiếp được).
- **`ImportBooks` tách lõi:** `CreateChapter` = `createChapter` (lõi, không refresh) + `reindexBookTOC`; vòng lặp import gọi `createChapter` rồi refresh **một lần** cho cả sách thay vì mỗi chương một lần.
- **`PUT` thiếu `status` âm thầm gỡ sách khỏi KB.** `UpdateBook` ép `Status=""` → `draft`, mà repo ghi cột `status` **tường minh** (không phải partial update) → mọi `PUT /chess/books/:id` thiếu trường này **hạ sách xuống bản thảo VÀ gỡ cả sách lẫn mọi chương khỏi kho**, không báo gì. Frontend an toàn (`BookLibrary.vue` có gửi `status`) nên lỗi này chỉ lộ qua script/`curl`. Sửa: dùng lại bản ghi đã đọc đầu hàm (trước bị bỏ bằng `_`) — `""` nghĩa là **không đổi**, không phải "reset về bản thảo". `CreateBook` giữ nguyên `"" → draft` (đúng cho sách mới).
- **`ReindexAll` không dọn sách bản thảo đã từng nạp.** Kịch bản rò: sách published (đã index) → tắt `CHESS_KB_INDEX` → hạ draft (lúc này `Remove` **early-return vì gate tắt**) → bật lại gate → reindex chỉ liệt kê `published` nên **không dọn** → nội dung chưa duyệt nằm lại kho vĩnh viễn. Sửa: sau khối published, liệt kê thêm sách `draft` và `Remove` sách + mọi chương; đếm vào field MỚI `Purged`. **Nhớ thêm `"purged"` vào `gin.H` của handler `ReindexKB`** — nó dựng thủ công, đúng cái bẫy đã làm rơi `articles_total` suốt từ Phase 1 tới PR #14.
- **Giao diện:** `BookLibrary.vue` thêm nút "Kho tri thức" (mở lại `ChessKBStatusDialog.vue` của PR #14, khỏi bắt Thầy sang tab khác) + huy hiệu trạng thái nay có tooltip/câu nói thẳng hệ quả ("Bản thảo — chưa vào kho tri thức, HLV AI không trích dẫn được").
- **Test:** thêm 5 case vào `chess_library_book_test.go` — giữ status khi PUT thiếu trường, hạ bản thảo tường minh vẫn có tác dụng, reorder ghi đúng thứ tự, reorder chặn sách không tồn tại, import sinh slug chương đủ (chống hồi quy cho việc đổi sang `createChapter`). **Không test được các lời gọi index**: `indexer` là **kiểu cụ thể** `*ChessKnowledgeIndexer` (không phải interface) nên test dựng service với `indexer: nil` → mọi nhánh index là no-op. Muốn phủ thật phải tách indexer thành interface — **backlog**, đụng cả struct service lẫn wiring.
- **Kiểm chứng:** `gofmt`/`go build`/`go vet` sạch trên mọi file đợt này. `npm run build` sạch; `npm test` 130/130 pass; `vue-tsc --noEmit` 0 lỗi. `go test` vẫn crash SIGSEGV tại `gojieba.NewJieba` (`internal/types/evaluation.go:18`) — **xác nhận lại lần nữa**: test CŨ `TestCreateBook_GeneratesSlug` crash y hệt, là lỗi môi trường Windows. Test mới chỉ được xác nhận **biên dịch sạch**; cần CI/Linux để kiểm runtime.

### Giao diện điện thoại 360–430px (2026-08-14)
Thầy báo mở phần mềm trên trình duyệt điện thoại thì khó xem/khó dùng. Rà ra: frontend **chưa từng được thiết kế cho mobile** — 3 chốt chặn cứng ở tầng khung khiến mọi thứ bên trong không co được, và cả 6 trang cờ dùng khuôn 2–3 cột `flex: 0 0 Npx`. **Ưu tiên Thầy chọn: ĐỌC nội dung đã soạn** (tra cứu khi ở CLB/lớp), người dùng là HLV/trợ giảng nội bộ.

**Ba quyết định kiến trúc (kiểm chứng bằng code, không suy đoán):**
1. **`.main` KHÔNG scoped** (`views/platform/index.vue` dùng `<style lang="less">` trần) → chốt chặn `min-width: 600px` gỡ được từ file CSS toàn cục, **0 dòng sửa** file đó.
2. **`menu.vue` (2069 dòng) + `stores/ui.ts` giữ 0 dòng sửa.** Sidebar → ngăn kéo làm HOÀN TOÀN bằng CSS ngoài nhắm `.main > .aside_box`. `menu.vue` dùng `v-if="!uiStore.sidebarCollapsed"` ở 7 chỗ để ẩn/hiện NỘI DUNG (CSS không cứu được), nên `composables/useMobileNav.ts` **"che" (shadow)** state: vào mobile gán TRỰC TIẾP `ui.sidebarCollapsed = false` (KHÔNG gọi `expandSidebar()` — action đó ghi localStorage, sẽ xoá ưu tiên desktop), ra desktop đọc lại localStorage.
3. **Width của `t-dialog`/`t-drawer` là style INLINE** → bắt buộc `!important`, và cũng vì thế MỘT luật toàn cục phủ hết **47 chỗ** dialog/drawer, kể cả drawer tính width bằng JS (`doc-content.vue` sàn 480/560px, `SettingDrawer.vue`) → 3 file upstream đó giữ 0 dòng.

⚠️ **CẢNH BÁO KHI MERGE UPSTREAM:** nếu upstream đổi tên class `.main`/`.aside_box`, hoặc đổi `.main` thành `<style scoped>`, thì **ngăn kéo mobile chết CÂM** (không lỗi, chỉ là sidebar 260px quay lại chiếm màn hình). Test `frontend/src/assets/theme/responsive.style.test.mjs` khoá đúng 2 class đó + khoá `menu.vue` không có `@media` — đó là lưới an toàn duy nhất.

- **File mới (fork-own, rủi ro merge 0):** `frontend/src/assets/theme/duongsinh-responsive.css` (khối A shell/ngăn kéo · B vô hiệu hack bù sidebar của `creatChat.vue`+`KnowledgeBase.vue` · C dialog · D drawer · E popup), `composables/useBreakpoint.ts` (`BP.mobile = 767`), `composables/useMobileNav.ts`, `components/MobileNavBar.vue` (thanh cố định 48px nền navy + ☰ + tiêu đề theo `route.name` + backdrop; đóng khi backdrop/✕/Esc/đổi route), `assets/theme/responsive.style.test.mjs`.
- **BA BẤT BIẾN của CSS mobile (có test khoá, đừng phá):**
  1. **Mọi `@media` phải có tiền tố `screen and`** — thiếu chữ `screen` là luật rò sang `@media print` của `BookPrint.vue`/`ArticlePrint.vue`, phá `.bkp-page { width: 180mm }` (lệch nửa pixel là Chrome đẻ trang trắng). Đã xác minh trong dist: 16/16 media query đều có `screen and`.
  2. **CẤM đơn vị viewport** (`vw`/`vh`/`dvh`…) trong CSS mới — `<html>` mang CSS `zoom` do `useFont.ts` ghi vào; dùng `100%` + `min-width: 0`. Cùng lý do `menu.vue` cố ý dùng `height: 100%` thay `100vh`. Riêng breakpoint thì dùng `matchMedia` an toàn (media query đánh giá ở tầng viewport, KHÔNG bị zoom nhân) — khác với ĐO/GHI toạ độ px, chỗ đó vẫn phải dùng `cssViewportSize()` của `utils/zoom.ts`.
  3. Breakpoint **767px** (không phải 600): khung tối thiểu cũ = sidebar 260 + chat 400 = 660px, đặt 600 thì dải 601–767 vẫn vỡ.
- **Trang cờ (file fork, 0 rủi ro merge):** khuôn **master–detail** — root nhận `:class="{ 'is-detail': !!selected }"` (state `selected` ĐÃ CÓ SẴN, không thêm state mới), mobile ẩn một trong hai khung + nút "‹ Danh sách". `ArticleBank.vue` 3 cột → cột chuyên mục 220px thành **dải chip cuộn ngang** (giữ điều hướng 1 chạm, vẫn dùng `filter.topic_id` sẵn có). Toolbar: **1 luật `display:grid` + `> * { width:auto !important }` cho cả cụm** thay vì sửa ~19 chỗ `style="width:NNNpx"` inline. Đổi `<div style="flex:1">` đệm → `class="xx-spacer"` để ẩn được trong grid.
- **Đọc thoải mái (lỗ hổng phát hiện khi làm):** khung xem trong app CHƯA có `img { max-width: 100% }`/`overflow-wrap`/`table` cuộn — trang in `BookPrint.vue` thì đã có. Ảnh Thầy tải lên từ máy ảnh (vài nghìn px) phá layout ngang. Đã thêm cho `.atb-content`/`.bkl-chapter-content`/`.cc-lesson-content`/`.pob-annotation`, **ngoài media query** vì đúng ở mọi kích thước.
- **Vùng chạm:** `.nav-btn` của `ChessBoardDisplay.vue` từ ~24px → 44px (nhãn nước đi xuống hàng riêng bằng `order:9; flex:0 0 100%` để 5 nút vừa 1 hàng: 5×44 + 4×8 = 252px); `.cpe-piece` của `ChessPositionEditor.vue` 36→44px với **`gap: 5px`** (6×44 + 5×5 = 289px, vừa cả ở cỡ chữ "Lớn" zoom 1.125 → chỉ còn ~292px khả dụng); nút icon trong hàng danh sách → 40px.
- **Hai va chạm đã phát hiện và né khi làm (đáng nhớ):**
  - **KHÔNG đụng `.move-list { max-height }`** trong nhánh mobile của `ChessBoardDisplay.vue`: `BookPrint.vue` đặt `:deep(.move-list){max-height:none}` với **CÙNG specificity (0,2,0)** → ai thắng phụ thuộc thứ tự chunk; nới khung cuộn ở đây có thể vô tình bóp lại danh sách nước đi khi xem trang in trên điện thoại.
  - **`ArticleBank.vue`: nút "‹ Danh sách" phải `v-if="!editing"`** — khung sửa của bài viết là INLINE (khác `BookLibrary.vue` dùng hộp thoại), bấm lùi giữa chừng sẽ **mất nội dung đang gõ mà không cảnh báo**.
- **Sửa kèm (đúng ở mọi kích thước):** `PuzzleBank.vue` `.pb-meta` thiếu `flex-wrap: wrap` (các trang cờ khác đều có); `ChessChapterHistory.vue`/`ChessArticleHistory.vue` bỏ `height: 60vh` ở mobile (đơn vị viewport sai dưới zoom) và xếp dọc, để `.t-dialog__wrap` lo cuộn.
- **Cố ý NGOÀI phạm vi:** không thêm `viewport-fit=cover` vào `index.html` (không có nó iOS tự giữ safe area — an toàn, 0 công; vẫn đặt `env(safe-area-inset-*)` phòng thủ trong CSS của ta, giá trị 0 nên vô hại); trang in giữ nguyên `180mm` + `overflow-x: auto` (cuộn ngang là hành vi ĐÚNG cho mô phỏng A4); 4 bảng `t-table` khu quản trị để cuộn ngang trong drawer; KHÔNG nạp lớp mobile vào `embed-main.ts` (widget nhúng đã fluid sẵn qua `.chat.is-embedded`); KHÔNG xoá 78 dòng media query chết ở `creatChat.vue`/`KnowledgeBase.vue` (khối B đã vô hiệu về chức năng, xoá chỉ thêm rủi ro merge).
- **Kiểm chứng:** `npm test` (`node --test`) **130/130 pass** (123 cũ + 7 mới). `npm run build` (vite) sạch. `vue-tsc --build --force` giữ nguyên **111 lỗi** nợ upstream — lọc riêng: lỗi duy nhất chạm file đợt này là `ChessPositionEditor.vue(56,29) POINTER_EVENTS`, **có sẵn từ commit position bank** (diff của đợt này vào file đó thuần CSS, không thể sinh lỗi import). Xác minh thêm trên **dist đã build**: luật `.main>.aside_box`, `html.ds-nav-open`, `.t-dialog__ctx .t-dialog{width:100%!important}` đều có mặt; 10 media query mobile trong chunk cờ mang đúng `[data-v-*]`.
- **CHƯA kiểm chứng được ở đây (Thầy phải tự xem):** máy này **không có Playwright/Puppeteer**, không chạy được trình duyệt. Toàn bộ kiểm chứng là TĨNH (test đọc source + soi dist). Cần Thầy mở thật và soi bằng DevTools device emulation: 360×740 · 375×667 · 412×915 · 430×932 · **360px ở cỡ chữ "Lớn"** (zoom 1.125 → chỉ 320 CSS px hiệu dụng, ca khó nhất) · 767 và 768 (biên) · **1440 (hồi quy desktop)**. Kiểm nhanh trong console mỗi route: `document.documentElement.scrollWidth <= document.documentElement.clientWidth`. Hồi quy bắt buộc: desktop bật thu gọn 60px → kéo hẹp xuống 360 → kéo lại 1440 ⇒ sidebar phải về 60px và `localStorage.sidebar_collapsed` không đổi.

**Backlog mới từ đợt này:**
- [ ] `ChessWikiLinkSuggest.vue:180-189` định vị popup sai dưới zoom (dùng `window.innerWidth` trần thay vì `cssViewportSize()` của `utils/zoom.ts`; `Input-field.vue:1271,1351` đã làm đúng) — lỗi CÓ SẴN, chỉ ảnh hưởng soạn thảo nên không thuộc ưu tiên "đọc".
- [ ] Tầng `compact` 768–1023px (tablet dọc) — hiện chỉ có 1 breakpoint.
- [ ] Vuốt cạnh trái để mở ngăn kéo (hiện chỉ bấm ☰).
- [ ] `t-table` khu quản trị chuyển sang thẻ (card) ở mobile — nếu sau này Thầy quản trị bằng điện thoại.

### Backlog cũ
- [x] Áp nhận diện thương hiệu Dương Sinh (`#2B3990` navy + xanh, logo) vào `frontend/` — xong WS4a (màu+logo+title). *Còn có thể làm thêm:* pattern ô cờ nền, font Roboto bundle (hiện chỉ promote trong font-stack).
- [ ] (Tùy chọn) Bật `CHESS_KB_INDEX` full stack + nối KB "Tri thức cờ vua" vào agent HLV — **runbook đã có:** `docs/chess-rag-enable.md`.
- [x] Nút "đổi tên slug" dùng `chess_slug_aliases` — xong WS-D2 cho game+puzzle (course/lesson còn lại). Position (mới) cũng có rename+alias.
- [ ] Ngân hàng thế cờ: rename cho course/lesson vẫn chưa có (nợ cũ, không phải phạm vi đợt này).
- [ ] Khi merge upstream lần tới: ưu tiên rà C1 (móc lõi) + C4 (i18n/prompt) + C6 (migration sqlite).
